{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/toadlyBroodle/skill-set/schema/skill-set.schema.json",
  "title": "Skill-set frontmatter",
  "description": "Validates the YAML frontmatter at the top of every SKILL.md, both transferable (in this repo) and proprietary (in a project's harness skills dir).",
  "type": "object",
  "required": ["name", "description", "version"],
  "additionalProperties": true,
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*[a-z0-9]$",
      "description": "kebab-case identifier; must match the folder name."
    },
    "description": {
      "type": "string",
      "minLength": 20,
      "description": "Human-readable summary; what the skill does and when it triggers. Must be specific enough that an LLM router can pick it from a list of similar skills."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[A-Za-z0-9.-]+)?$",
      "description": "SemVer (MAJOR.MINOR.PATCH, optional pre-release suffix)."
    },
    "user-invocable": {
      "type": "boolean",
      "description": "If true, the user can run this skill directly via the harness's slash-command surface. If absent or false, the skill is only invoked indirectly (by the chain runner, supervisor, or another skill)."
    },
    "transferable": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*[a-z0-9]$",
      "description": "PROPRIETARY skills only: the name of the transferable counterpart in the master repo. Transferable skills omit this field."
    },
    "transferable-version": {
      "type": "string",
      "description": "PROPRIETARY skills only: npm-style version range pinning the transferable counterpart (e.g. '>=1.0.0', '^1.2.0', '~1.0.5')."
    },
    "model-floor": {
      "type": "string",
      "enum": ["opus", "sonnet", "haiku"],
      "description": "Phase 19 model-tier routing: lowest model tier this skill is ever allowed to run on, regardless of item difficulty. Resolved at iter-start as `effective_model = max(item.model_tier, skill.model_floor)` over the order {haiku < sonnet < opus}. Optional; defaults to 'opus' when absent. Framework-level skills (sst-supervisor, sst-sanitize-transferable) pin 'opus'; review-class skills typically 'sonnet'; mechanical / well-bounded skills 'haiku'."
    },
    "effort-floor": {
      "type": "string",
      "enum": ["low", "medium", "high", "xhigh", "max"],
      "description": "Phase 19 effort-tier routing: lowest reasoning-effort tier this skill is ever allowed to run on, regardless of item difficulty. Resolved at iter-start as `effective_effort = max(item.effort_tier, skill.effort_floor)` over the order {low < medium < high < xhigh < max}. Optional; defaults to 'high' when absent (matches the API-standard default; Opus 4.7's CLI implicit 'xhigh' is downshifted by the runner's pinned `--effort high`). Framework-level skills pin 'xhigh'; review-class 'high'; mechanical 'medium' (recorded for forward-compat with future Haiku tiers that support effort)."
    }
  },
  "allOf": [
    {
      "if": { "required": ["transferable-version"] },
      "then": { "required": ["transferable"] }
    }
  ]
}
