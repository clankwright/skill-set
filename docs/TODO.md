# skill-set TODO (handoff doc)

> Cross-cycle state. Every skill reads this on start and updates it on close. Three sections, in this order. Primary spec: `docs/SPEC.md`.

## In flight

<!--
  Exactly one line per currently-running skill, format:
  - [<skill-name> @ <utc-iso>] <one-line: what this skill is currently doing>
  Rewrite (don't append) as the focus narrows. Empty when no skill is running.
-->



## Just shipped (last cycle)

<!--
  Append-on-close, newest first. Format:
  - <one-line summary> — by <skill-name> at <utc-iso>
  No commit SHA: a commit cannot contain its own hash, and amend-based
  workarounds produce stale references. Correlate entries to commits via
  `git log --oneline --grep '<keyword>'`. Older entries below retain their
  SHAs from the prior two-commit pattern; leave them alone, they're valid.
  Trim to the most recent 10 entries; older history lives in docs/SPEC.md
  phase blocks and `git log`.
-->

- Phase 22 [medium] `sst-dev-review` v1.3.2 → v1.4.0: batch-coherence axis (§2.9) + batch-sizing axis (§2.10) — coherence checks `[batch-pick]` items vs diff + `[x]` flips + Just-shipped entries, files `[batch-coherence]` finding on unrelated bundling; sizing reads MANIFEST input tokens, computes per-difficulty band edges (easy 100-200k / medium 200-300k / hard 400-500k), files `[batch-sizing]` finding on under/oversized cycles. Inline sanitize: must-fix=0, should-fix=0, nit=0. (by skill-set-dev at 2026-04-28T00:22:42Z)
- Phase 22 [medium] `skill-set-dev-review` v1.1.4 → v1.2.0: mirrored batch-coherence (§2.10) + batch-sizing (§2.11) from transferable with framework-specific paths (`00_skill-set-dev.txt` transcript grep, `bin/validate-frontmatter.py` axis-count in §6 report bumped 9→11). Applied via `bin/apply-skill-patch.py`. (by skill-set-dev at 2026-04-28T00:22:42Z)
- Phase 22 [hard] `sst-dev-cycle` v1.3.1 → v1.4.0: replaced "Small scope per cycle" operating principle with "Difficulty-windowed batching"; rewrote §1 around primary-pick + batch-additions + window-target + `[batch-pick]` sentinel emit. Token windows by difficulty: easy 100-200k, medium 200-300k, hard 400-500k input tokens. Single-commit rule unchanged; bundling unrelated items still forbidden. Emission order at iter start documented (TodoWrite → In-flight → `[batch-pick]` → `[picked-difficulty]` → §2). Inline sanitize: must-fix=0, should-fix=0, nit=0 (caught + scrubbed two `Phase 19`/`Phase 22` leaks during the inline pass; only generic process tokens — `batch-pick`, `window-target`, `rationale`, `primary`, `at-or-below` — landed in transferable prose). Validator clean (24 skills + 7 chains). (by user-directed manual cycle at 2026-04-27T23:57:15Z)
- Phase 22 [hard] `skill-set-dev` v1.2.4 → v1.3.0: synced batching contract from transferable; replaced "Right-size the cycle (~200k-400k)" operating principle with the per-difficulty windows and chunk-shape estimates feeding into them; rewrote §1 with primary-pick + batch-additions + `[batch-pick]` declaration mirroring the transferable's shape; preserved repo-specific selection priority (Next up → latest Phase block → older Phase tail → user directive). Validator clean (5 proprietary skills + 2 proprietary chains). (by user-directed manual cycle at 2026-04-27T23:57:15Z)
- sst-chain-driver v1.2.0 → v1.2.1: dropped internal phase-number references from Worker-lifecycle section heading and prose; tightened verbs to present tense matching implemented state. Inline sanitize: must-fix=0, should-fix=0, nit=0. Closes Phase 18 review follow-up. Validator clean (24 skills + 7 chains). (by skill-set-dev at 2026-04-27T14:33:54Z)
- Backfill Just-shipped for commit 4fceb04: chains/dev-cycle-with-review-looped.yaml v1.1.0→v1.2.0 (loop-delay-random [60,3600]→[60,600]) + chains/dev-cycle-overnight.yaml v1.0.0→v1.1.0 ([300,7200]→[60,1800]). Bundled with sst-chain-driver sanitize fix in one cycle per CLAUDE.md single-commit rule. Closes Phase 21 review follow-up. (by skill-set-dev at 2026-04-27T14:33:54Z)
- Phase 21 closed end-to-end: user→manager→supervisor steering channel via Telegram `/feedback <message>`. Three coordinated pieces in one commit. (a) `bin/manager-bot.py`: extends `KNOWN_COMMANDS` with `feedback`; new `queue_feedback(body, chat_id)` helper writes `<utc-filestamp>_feedback.json` with shape `{command: "feedback", body, received_at, from_chat_id}` and disambiguates same-second collisions via a `-N` suffix loop (feedback-scoped because feedback bodies are unique user input — pause/resume collisions are masked by idempotency, so the existing `queue_task` shape stays untouched); `handle_command` matches `/feedback` (with optional `@botname` suffix) via `re.match(r"^/feedback(?:@\S+)?(?:\s+(.*))?$", text, flags=re.DOTALL)` so multi-line bodies are preserved verbatim — the legacy `text.lstrip("/").split()` would corrupt newlines + collapse whitespace runs; empty body returns usage hint instead of an empty queue file; `/help` text adds the new command. Reply: `Queued feedback (N chars). Next manager run will route it to the supervisor.`. Smoke-tested four cases pre-commit: single-line body, multi-line body with embedded newlines, empty `/feedback`, `/feedback@my_bot do X please` (botname suffix stripped, body extracted correctly); a back-to-back triple-submission test confirmed all three messages preserved on disk via the `-N` suffix path. (b) `sst-manager` v1.2.0 → v1.3.0: frontmatter description gains "(including user feedback routed onward to the supervisor)"; Inputs table gains `~/.claude/state/manager-feedback.md` row (`yes` read, `yes` write — append newest-first, ~2KB cap); §1 queue-command-types gains a second JSON shape and a `feedback` handler row; new "Routing feedback to the supervisor" sub-section codifies the four-step routine — read-or-create with H1 + lead paragraph, prepend `## <utc-iso> from <chat-id>` block with body verbatim, trim oldest from the bottom until under ~2KB, delete queue file last (transient failure → leave queue file for retry, no `.error` sibling). Manager NEVER paraphrases the body; pure capture-and-route. Feedback distinct from `manager-guidance.md`: guidance is manager-derived patterns ("the last 3 cycles each spent >100k tokens on the deploy step"); feedback is direct user-to-supervisor messaging ("stop tagging skills with `[easy]` until the harness honors the floor table"). Supervisor weighs both; feedback wins on conflict. (c) `sst-supervisor` v1.6.1 → v1.7.0: §Inputs gains a new step 5 between the existing manager-guidance read and the SPEC/TODO read; supervisor treats feedback entries as authoritative steering distinct from (stronger than) manager-guidance; feedback directives are valid motivating citations for §3's change-intent table (citation column reads `manager-feedback.md:<line>` rather than a transcript line — the framework's first allowance for non-transcript citations, justified by user-as-author); conflict resolution explicit (feedback > manager-guidance; `auto-promote` mode > feedback); anti-fork rules still bind (feedback asking the supervisor to skip sanitize on a transferable, commit code, or deploy is REFUSED, recorded in `## Notes for the manager`). Inline sanitize judgment per transferable: only generic process descriptions added (`feedback`, `manager-feedback.md`, `## <utc-iso> from <chat-id>`, framework-canonical schema enum tokens, generic-N placeholder phase numbers like "phase N"); no project nouns, no banned-list terms (toadlyBroodle/landonmutch/sdrai/csvagent/botlab/satring/sn-bot/akiya/vultr/mpp/lnbits/phoenixd/rc.d/gunicorn/protonmail all absent), no internal phase numbers in transferable prose (Phase 21 lives in SPEC.md only — neither transferable references the phase number), no proprietary paths, no user-identifying details. Per-file verdict: must-fix=0, should-fix=0, nit=0 across both transferables. Validator clean (24 skills + 7 chains). SPEC.md gains a new "### Phase 21" block (sits between Phase 19 and Phase 20 in section ordering since Phase 20 is deferred) with all three sub-items flipped `[x]` and a closing paragraph noting that the phase preserves every existing invariant (manager remains read-only, supervisor never commits/deploys, sanitization gate untouched) while adding a single new write path: bot → queue file → manager-feedback.md → supervisor's input set. (by skill-set-dev at 2026-04-27T13:54:28Z)
- sst-manager v1.1.0→v1.2.0: rewrote §4 digest format to plain-language progress updates (language rules + three representative examples calibrating digest tone: objective-progress+issue+fix, clean tick, empty-queue); simplified escalation template; fixed pre-existing Worker-lifecycle "Per the Phase 18 lifecycle policy" phrase (sst-manager portion of Phase 18 open should-fix). Sanitize: must-fix=0, should-fix=1 fixed inline, nit=2. Validator clean (24 skills + 7 chains). (by skill-set-dev at 2026-04-27T13:01:26Z)
- Synced skill-set-dev-review v1.1.3 → v1.1.4: ported §4 difficulty-label format + §5 Co-Authored-By hoist from sst-dev-review v1.3.2; Phase 19 review-follow-up `[x]` (by skill-set-dev at 2026-04-27T12:20:35Z)
- Phase 19 #7 closed end-to-end: tagged every preamble-listed transferable + proprietary SKILL.md with `model-floor:` + `effort-floor:` in one commit. 12 transferables touched (5 Sonnet+high: `sst-dev-review` v1.3.1 → v1.3.2, `sst-skill-router` v1.1.0 → v1.1.1, `sst-editorial-pass` v1.0.0 → v1.0.1, `sst-iterative-writer` v1.0.0 → v1.0.1, `sst-literary-critic` v1.0.0 → v1.0.1; 7 Haiku+medium: `sst-translator` v1.0.0 → v1.0.1, `sst-fact-checker` v1.0.0 → v1.0.1, `sst-promote-skill-proposal` v1.1.0 → v1.1.1, `sst-output-selector` v1.0.0 → v1.0.1, `sst-llm-judge-ranker` v1.0.0 → v1.0.1, `sst-email-control-loop` v1.0.0 → v1.0.1, `sst-setup-telegram` v1.0.0 → v1.0.1) + 2 proprietary counterparts mirrored to their transferable's class (Sonnet+high: `skill-set-dev` v1.2.2 → v1.2.3, `skill-set-dev-review` v1.1.2 → v1.1.3; applied via `bin/apply-skill-patch.py` per `.claude/`-scoped permission gate). Already-tagged from prior cycles (no-op this cycle): `sst-supervisor` (Opus+xhigh), `sst-sanitize-transferable` (Opus+xhigh), `sst-dev-cycle` (Sonnet+high), `skill-set-supervisor` (Opus+xhigh, proprietary). Out-of-scope (not in the preamble's floor table; defaults `opus`/`high` continue to apply): `sst-manager`, `sst-web-research`, `sst-domain-seo-research`, `sst-short-video-generator`, `sst-lead-generation`, `sst-linkedin-easy-apply`, `sst-linkedin-networking`, `sst-social-promoter`, `sst-chain-driver` (no transferable file in this repo), and the proprietary `skill-set-chain-driver`. Tagging these would require extending the preamble's floor classification first per the anti-fork rule, deferred to a future cycle. Inline sanitize judgment per transferable: only framework-canonical schema enum tokens added (`sonnet`, `haiku`, `high`, `medium`) plus a SemVer patch bump; no project nouns, no banned-list terms (toadlyBroodle/landonmutch/sdrai/csvagent/botlab/satring/sn-bot/akiya/vultr/mpp/lnbits/phoenixd/rc.d/gunicorn all absent), no internal phase numbers, no proprietary paths, no user-identifying details. Per-file verdict: must-fix=0, should-fix=0, nit=0 across all 12. Validator clean across both passes (auto-walk: 24 skills + 7 chains; explicit-paths on `.claude/skills/skill-set-*/SKILL.md` + `.claude/chains/*.yaml`: 4 skills + 2 chains). SPEC Phase 19 item #7 (line 323) flipped `[ ]` → `[x]` with the closure paragraph documenting the per-class assignments + scope-boundary rationale + sanitize verdict. Phase 19 has one open item remaining: #9 (`[hard]` acceptance with mixed-difficulty items across a `--loop 3` run — gated on Phase 17/18 acceptance prerequisites which themselves require live Telegram chain runs already queued in Next up). (by skill-set-dev at 2026-04-27T11:20:55Z)

## Next up (queued for next cycle)

<!--
  One line per queued item. The next cycle picks the top item unless the spec says otherwise.
  Format:
  - <one-line description> — <reason/source: spec phase X.Y, supervisor verdict <sha>, manager directive, user message>
  Order: blockers/highest-impact first.
-->

- [hard] Phase 22 `sst-supervisor` batch-window refinement loop. Reason: spec Phase 22 sub-item #7; new §X.Y self-tune step reads recent `[batch-sizing]` findings (#5) + iter MANIFEST actuals; on N consecutive same-direction findings (default N=3) OR M total findings in trailing 20 iters (default M=5), writes a SKILL.md patch to `sst-dev-cycle` (+ proprietary mirror) refining chunk-shape estimates, band edges, or adding empirical entries. Patches land per `auto-promote` mode (proprietary direct-overwrite default; transferable on `all` via sanitize gate; sidecar on `off`). Stable termination: K consecutive iters (default K=10) with zero findings → supervisor stops patching, monitors only; re-engages on next finding. Makes window-targets self-correcting rather than honor-system. Bump.
- [hard] Phase 22 `skill-set-supervisor` mirror sync of the refinement loop. Reason: spec Phase 22 sub-item #8; same self-tune step in the proprietary supervisor with this repo's banned-terms additions + project-locations references.
- [medium] Phase 18 `acceptance check on chain-bound worker lifecycle`. Reason: spec Phase 18 item 5; the implementation landed this cycle (item 4 `[x]`) but acceptance requires a real `/skill-set-chain-driver` round-trip with Telegram. Confirm: (a) chain driver starts the `skill-set-bot` tmux session at session-start and the user can `/ping → pong` during the run; (b) chain driver kills the tmux session at session-end; (c) re-run with the worker pre-started by hand (e.g. `tmux new-session -d -s skill-set-bot ...`): chain driver detects the pre-existing worker, does NOT start a second one, does NOT kill it at session-end; (d) two simultaneous chain-driver runs against different chains: only one tries to start the worker (flock); (e) manager invocation while no chain is running succeeds with an empty inbound queue and a digest body that does NOT re-notify any currently-paused job.
- [easy] Phase 17 `acceptance check on empty-queue bail`. Reason: spec Phase 17 item 4; the steady-state run requires `Next up` empty AND every SPEC `[ ]` flipped `[x]` (this cycle didn't reach that state, so item 4 stayed `[ ]`). When the queue eventually empties, invoke `/skill-set-chain-driver` and confirm: (a) `skill-set-dev` exits 0 with `[no-work] ...` printed and no commit; (b) chain runner aborts the loop after the first empty iter (no second iter starts; `MANIFEST.loop.terminated_by == "no_work_bail"`); (c) iter MANIFEST records `no_work_bail` with the sentinel line; (d) cumulative cost is bounded by the dev skill's pre-flight reads (~$0.10-0.30) rather than a full ~$8 iter; (e) chain-driver session-end Telegram body labels the stop "no-work bail".
- [medium] Phase 14 `Acceptance check: kill -TERM mid-supervisor + verify self-heal`. Reason: spec Phase 14 close item; one combined acceptance test for the four Phase 14 mechanisms above.
- [medium] Phase 20 `goose-cerebras harness` (deferred). Reason: spec Phase 20 (deferred behind Phase 19); the per-skill model-tier work in Phase 19 delivers the bulk of the 24/7-productivity throughput win within the existing Max subscription, so this becomes a future supplement for free-tier capacity rather than the primary fix. Re-pick when Phase 19 acceptance ships and a real cost / throughput baseline shows Phase 19's win is insufficient AND mechanical-skill work has been cleanly identified for offload to Cerebras. Architecture as in spec: `GooseCerebrasHarness` subclass + `bin/goose-shim.py` translating Goose's stream-json event vocabulary to Claude Code's shape + per-skill `allowed-harnesses:` anti-fork guard.
- [medium] Phase 8.6 end-to-end smoke: chain `sst-web-research → sst-editorial-pass → sst-social-promoter` against a real consuming project with clean supervisor verdict. Reason: spec Phase 8 tail item, Phase 8.1-8.5 lifts already complete.
