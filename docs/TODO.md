# skill-set TODO (handoff doc)

> Cross-cycle state. Every skill reads this on start and updates it on close. Three sections, in this order. Primary spec: `docs/SPEC.md`.

## In flight

<!--
  Exactly one line per currently-running skill, format:
  - [<skill-name> @ <utc-iso>] <one-line: what this skill is currently doing>
  Rewrite (don't append) as the focus narrows. Empty when no skill is running.
-->

## Just shipped (last cycle)

<!--
  Append-on-close, newest first. Format:
  - <one-line summary> — by <skill-name> at <utc-iso>
  No commit SHA: a commit cannot contain its own hash, and amend-based
  workarounds produce stale references. Correlate entries to commits via
  `git log --oneline --grep '<keyword>'`. Older entries below retain their
  SHAs from the prior two-commit pattern; leave them alone, they're valid.
  Trim to the most recent 10 entries; older history lives in docs/SPEC.md
  phase blocks and `git log`.
-->

- Phase 15 rename for clarity: `sst-orchestrator` → `sst-chain-driver` (single-session driver of one multi-iter chain run; v1.0.0 → v1.1.0); `sst-agent-orchestrator` → `sst-skill-router` (in-process per-task planner inside one user request; v1.0.0 → v1.1.0); helper `bin/orchestrate-chain.py` → `bin/drive-chain.py`. `sst-manager` unchanged (cross-project cron-based ops loop fits the everyday meaning of "manager"). Motivation: "orchestrator" appearing on two distinct skills made conversational references ambiguous and the everyday English meaning overlapped with both. New names lead with mechanism (drive a chain run / route to skills / monitor projects) rather than a management metaphor. SKILL.md frontmatter `name:` updated; body prose rewritten (skill heading, "the orchestrator" prose throughout, hard-rules block, command-line shape examples); both renamed skills carry a "Naming history" footer. `bin/drive-chain.py` internal references updated (docstring, `prog=` argparse name, all `[orchestrator]` log-line tags → `[chain-driver]`, runtime Telegram body prefixes `chain-driver: session START` / `chain-driver [<label>]: ...` / `chain-driver: session END`). `sst-email-control-loop` description's "called by sst-agent-orchestrator" reference updated to point at `sst-skill-router`. Forward-looking SPEC.md items in Phase 12 + Phase 13 acceptance updated to new names; historical closed Phase 12 #5 / Phase 13 #6 entries left intact (the historical record is correct as written). New Phase 15 SPEC block documents the rename + rationale + migration list. Stale deployed copies at `~/.claude/skills/sst-orchestrator/` + `~/.claude/skills/sst-agent-orchestrator/` removed manually after `bin/install-skills.sh -y` (the install script intentionally does not delete target-only dirs to protect hand-managed skills, so renames require an explicit cleanup step). Validator clean (24 skills + 6 chains). No proprietary `<persona>-orchestrator` counterpart existed in any consuming project, so no external cascade — by claude-code at 2026-04-26T01:55:00Z
- Phase 6 push-to-GitHub closed: `origin` set to `git@github.com:toadlyBroodle/skill-set.git`, `git push -u origin main` published all history through `a45a506`, `main` now tracks `origin/main`. Closes the standing supervisor "push step is no-op" observation that's been carried in `Next up` since the framework gained an `install-skills.sh` push call. SPEC Phase 6 `Push to public GitHub repo` flipped `[ ]` → `[x]`. CONTRIBUTING.md and CI items in Phase 6 already `[x]` from prior cycles. No code change; remote add + push only — by claude-code at 2026-04-26T01:27:00Z
- Phase 12 efficiency wins #1 + #2 bundle (same-root carveout + tagging): closes the upstream/downstream coordination gap where `sst-dev-review` correctly identifies a multi-surface follow-up but each entry then gets picked atomically and pays per-cycle review+supervisor overhead twice. `sst-dev-cycle` v1.0.3 → v1.1.0 §1 gains a "Same-root bundling" paragraph: when two or more `## Next up` entries carry a `(group with <root-keyword>)` tag AND combined diff <~300 LoC AND files are disjoint, bundle the tagged set into one cycle; mismatched tags / breach of LoC / independent acceptance criteria fall back to per-item picks; untagged items still picked individually. `sst-dev-review` v1.1.0 → v1.2.0 §4 gains a "Same-root tagging" paragraph: when multiple findings share a single root cause (constant propagating to multiple surfaces, sibling-module guard, manifest+README discovery drift), append `(group with <root-keyword>)` to each TODO.md line using the exact same token across every entry (string-equality is the bundling key). Single-finding "groups" are not tagged. Spec entries never get the tag (the spec's one-checkbox-per-finding rule is unchanged; bundling is a TODO-level scheduling concern). The two changes were themselves bundled per the new same-root rule (one root: sst-dev-cycle/-dev-review coordination), prose-only, file-disjoint, well under 300 LoC combined. Sanitization judgment pass on both additions: must-fix=0, should-fix=0, nit=0 (only generic example tags `input-bound-propagation` / `manifest-readme-sync` / `auth-helper-migration`; no proprietary nouns; cross-refs only to transferable counterparts; the empirical Bearer-min-fee-floor motivation stays in this repo's SPEC.md and out of the transferable prose). Validator clean (24 skills + 6 chains). SPEC Phase 12 efficiency-wins items 1 and 2 flipped `[ ]` → `[x]` — by skill-set-dev at 2026-04-26T01:30:00Z
- Phase 14 #4 housekeeping helper shipped: `bin/clean-skill-runs.py`, idempotent dry-run-by-default cleanup over `<project>/.skill-runs/` + `<project>/.claude/skills/`. Three classes of cruft: (1) empty `<run-dir>/proposals/` (and per-iter `iter_NN/proposals/`) dirs, the pre-Phase-11 skeleton leftovers; (2) `*SKILL.md.bak` files anywhere under `<project>/.claude/skills/`, the supervisor's prior `--backup` cruft; (3) `<run-dir>/drafts/` whose newest FILE mtime is older than `--days` (default 14), prompt-per-dir unless `--yes`. Empty drafts dirs always qualify regardless of threshold. Approved-root safety mirrors `apply-skill-patch.py`: refuses any root outside the user's home OR lacking both `.skill-runs/` and `.claude/skills/`. Critical fix during impl: `latest_file_mtime` walks files only and ignores the directory's own mtime, because `<draft.md>`'s `touch -d <old>` doesn't backdate the parent dir; aging by parent mtime would have masked every `<old-content-but-newly-touched-dir>` case the helper exists to handle. CLI: `bin/clean-skill-runs.py [--apply] [--yes] [--days N] [<project-root>]`. Test matrix: synthetic fixture flags exactly the right items (115d-old content gets caught, today's content doesn't, non-empty proposals/ left intact); prompt-`n` keeps stale drafts; `--yes` clears them; second run is no-op (idempotency). Real-world dry-run on this repo + sdrai surfaces 1 empty drafts/ + 1 empty proposals/, no `.bak` (already one-shot cleaned in Phase 14 #3); user runs `--apply` later. SPEC Phase 14 item 4 flipped `[ ]` → `[x]` — by skill-set-dev at 2026-04-26T00:55:00Z
- Phase 12 review follow-up (orchestrator looping detection): `bin/orchestrate-chain.py`'s `looping` flag was previously set once at startup from `(args.loop is not None and args.loop != 1)`, so when `chains/dev-cycle-with-review-looped.yaml` was invoked without re-passing `--loop 3` on the CLI (the canonical no-override invocation, since the YAML already declares `loop: 3`), the chain runner resolved `loop_count = 3` and wrote `iter_NN/MANIFEST.json` plus iter banners, but the orchestrator's `_finalize_iteration` read the flat top-level `MANIFEST.json` (which only exists at session-end), per-iter telegrams fell to the "MANIFEST.json not found" branch, cumulative cost stayed $0.0000, and the `--max-budget-usd` halt never fired. Fix promotes `looping = True` inside the iter-banner handler BEFORE calling `_finalize_iteration(n - 1)`. Banner presence is authoritative because `bin/skill-chain.py:958-964` only prints `===== iteration N =====` when its resolved `total_iterations != 1`; YAML-vs-CLI source of loop_count is irrelevant once a banner appears. Closure pattern verified — `_finalize_iteration` and `_supervisor_verdict_path` read `looping` from the enclosing scope at call time, so reassignment in main() propagates without `nonlocal`. Four inline scenarios cover the banner-regex matrix (`N/M` with totals, the `(looping until failure)` infinite label, the ANSI-prefixed line stripped before the regex runs), the verdict-path layout flip (flat vs `iter_NN/`), and the closure-promotion sequence (write `looping=True`, then a nested `finalize` call resolves to the iter_NN path). SPEC Phase 12 review-follow-up `[ ]` → `[x]`. Validator clean (24 skills + 6 chains) — by skill-set-dev at 2026-04-26T00:45:00Z
- Phase 12 #5 + Phase 13 #6: shipped `sst-orchestrator` (`skills/framework/sst-orchestrator/SKILL.md` v1.0.0) + `bin/orchestrate-chain.py`. The helper spawns `bin/skill-chain.py --chain <name> --loop N` as a subprocess, streams stdout verbatim, and runs a parallel watcher that fires Telegram bodies via `bin/notify-telegram.sh` at five event classes: session-start (chain + caps + log-dir), iter-close on each `===== iteration N =====` banner (reads prior `iter_NN/MANIFEST.json` for cost/SHA/verdict + `git log -1 --pretty=%s` for the commit subject + counts `rate_limit_pauses` from the iter manifest), rate-limit pause/resume parsed from the chain runner's `[rate-limit]` banners (closes Phase 13 #6 in same cycle), halt-request when `--max-budget-usd` / `--max-cycles` / `supervisor_verdict.md` outcome=`escalate` thresholds fire, and session-end. Halts SIGINT the chain runner between iterations; the runner's existing `KeyboardInterrupt` path finalizes manifests cleanly. CLI shape: `--chain <name> --loop N --max-budget-usd $X --max-cycles N --telegram-env <path> --no-telegram --label <name> [-- <chain-runner-extras>]`. Telegram credentials sourced from `--telegram-env` file (KEY=VALUE shell env, optional `export ` prefix, single/double-quote stripping); falls back to environment variables. Lives under `skills/framework/` next to `sst-manager`; explicitly disambiguated from `sst-agent-orchestrator` (in-process per-task planner) in the description. Sanitization judgment pass: must-fix=0, should-fix=0, nit=0 (placeholders `<persona>` / `<project>` / `<chain-name>`; only canonical framework paths cited; only example chain name is the transferable `dev-cycle-with-review-looped`). Validator clean (24 skills + 6 chains). Smoke-tested: bogus-chain invocation propagates the chain runner's resolution failure, telegram-suppression mode prints `[suppressed]` tags to stderr without erroring. Installed via `bin/install-skills.sh -y`; harness skill registry now lists `sst-orchestrator`. SPEC Phase 12 item 5 + Phase 13 item 6 flipped `[ ]` → `[x]`. Proprietary `<persona>-orchestrator` counterpart deferred to a follow-up cycle (queued in Next up) — by skill-set-dev at 2026-04-26T00:30:00Z
- Phase 9 + 11 + 12 chain-YAML/README docs bundle: documented every chain-YAML field on the user-facing surface and exercised the auto-promote enum across the transferable chain set. README.md gains a "Chain YAML fields" section with an annotated example block + per-field subsections covering loop mode (with the `iter_NN/MANIFEST.json` log layout), auto-promote (full routing table + when-to-pick guide), and rate-limit handling (pause / pause-with-cap / fail with the resolved-policy + max-pauses-per-session bounds). Every existing transferable chain under `chains/` gained an explicit `auto-promote`: `dev-cycle-with-review` → `proprietary`, `editorial-with-fact-check` / `multi-output-evaluation` / `research-and-write` / `research-write-promote` → `"off"` (all bumped to v1.1.0 to mark the explicit-default landing). New `chains/dev-cycle-with-review-looped.yaml` (v1.0.0, `loop: 3`, `auto-promote: all`) ships a multi-iteration variant of `dev-cycle-with-review` so a single invocation knocks out the next three queued items autonomously while letting the supervisor's transferable improvements land within the same run (sanitize gate blocks proprietary leakage). Caught and noted a YAML-1.1 trap: PyYAML parses bare `off` as boolean false and trips the schema enum, so `auto-promote: off` must be quoted. README example flags it inline; the schema's enum-check is the standing guardrail. SPEC Phase 9 items 5/6, Phase 11 explicit-auto-promote item, and Phase 12 efficiency-win 3 (adopt loop mode on a transferable chain) all flipped `[ ]` → `[x]`. Validator clean (23 skills + 6 chains) — by skill-set-dev at 2026-04-26T00:00:00Z
- Phase 14 item 5 randomized inter-iteration delay shipped: `loop-delay-random: [min_seconds, max_seconds]` lands on `schema/skill-chain.schema.json` (with `allOf` mutual-exclusion against `loop-delay`) and `bin/skill-chain.py` (CLI `--loop-delay-random "<min>,<max>"`, parses + range-checks both YAML and CLI sources, samples `random.uniform(min, max)` per iteration boundary). Top-level `MANIFEST["loop"]` gains `delay_random_range` + `delay_samples` (one entry per boundary, audit trail). `[loop] sleeping Ns (sampled from [min, max])` banner shows each chosen delay live; existing `time.sleep`/`KeyboardInterrupt` path keeps Ctrl-C clean. Schema test matrix covered: mutual-exclusion fires, malformed shapes rejected (`[60]`, `[60,3600,1]`, `[-5,60]`), back-compat preserved for chains with neither field or only loop-delay. Proprietary `~/Dev/skill-set/.claude/chains/skill-set-cycle.yaml` v1.0.0 → v1.1.0 sets the default `[60, 3600]` so this repo's own multi-iter runs jitter 1-60min between iterations without an explicit CLI flag — addresses the user's standing observation that no random wait was firing between agent sessions. Phase 14 SPEC item 5 flipped `[ ]` → `[x]` — by claude-code at 2026-04-25T23:30:00Z
- Phase 14 supervisor completion-contract bundle: §0.6 iter-boundary drafts sweep + §8 exit gate + drop `--backup` default + Operating-principle bullet, in `sst-supervisor` SKILL.md (v1.4.1 → v1.5.0). §0.6 detects `MANIFEST.iteration > 1` and scans `<base>/iter_<NN-1>/drafts/` for orphans, treats each as a manager-injected finding citing the prior iter's supervisor transcript line, routes per current `auto-promote` mode, re-sanitizes transferable drafts before applying (prior-iter sanitize pass doesn't bind this iter), drops consumed drafts. §8 verifies before returning that (a) every file in `<run-dir>/drafts/` is either applied or named in a `[deferred]` block in the verdict, AND (b) the verdict file exists even on clean runs (a clean run with no verdict is indistinguishable from a partial-completion failure). New `[deferred]` block format documented for the verdict's "Updates written" section. `--backup` removed from both §3 invocation template + §Permissions example (helper still accepts it for ad-hoc human use). One-shot cleanup deleted the 3 historical `.bak` files (1 in sdrai's `.claude/skills/`, 2 in this repo's `.claude/skills/`). SPEC Phase 14 items 1, 2, 3 flipped `[ ]` → `[x]`. Bundled as one cycle because all three share root cause (supervisor completion contract + run-dir hygiene), touch the same SKILL.md in disjoint sections, and Phase 14 acceptance check (item 8, still open) tests them jointly. Validator clean; sanitize judgment pass on additions: must-fix=0, should-fix=0, nit=0 (one in-flight `sdrai` mention caught and rewritten before commit) — by skill-set-dev at 2026-04-25T22:30:00Z
- Phase 13 review follow-up: joint-fire merge condition fix in `bin/skill-chain.py`'s `run_skill_with_retry`. Previously the text-extracted `rate_limit_text_reset` (e.g. `7:50pm (Asia/Tokyo)`) was only threaded into the synthetic signal when `signal is None` — but the canonical live-failure mode this exists to fix has BOTH a structured `rejected` event AND a stderr `out of extra usage … resets …` line firing, so when the structured payload omitted `reset_time` under all four aliased field names the wake time fell through to the 300s exponential backoff floor (the very regression Phase 13 was meant to prevent). Changed merge condition from `if signal is None:` to `if not eff_signal.get("reset_time"):` so the wall-clock fills in whenever the structured signal lacks a parseable reset, regardless of whether the structured signal fired. Tracking var `text_reset_used` lets the manifest pause-record `source` field report mixed provenance: `"rate_limit_event+text_reset"` (joint-fire merge), `"rate_limit_event"` (pure structured), `"text_fallback"` (stderr-only-death). Empty-string and None-valued `reset_time` fields on the structured payload are now treated as falsy and overridable by `text_reset` (covering the realistic case where the harness emits the field but with no value). Eight inline scenarios verified the full matrix; SPEC Phase 13 review-follow-up flipped from `[ ]` to `[x]` — by claude-code at 2026-04-25T21:30:00Z

## Next up (queued for next cycle)

<!--
  One line per queued item. The next cycle picks the top item unless the spec says otherwise.
  Format:
  - <one-line description> — <reason/source: spec phase X.Y, supervisor verdict <sha>, manager directive, user message>
  Order: blockers/highest-impact first.
-->

- Phase 13 `Acceptance check: Telegram pause/resume leg only` — reason: spec Phase 13 close item; items (a)-(e) verified live on 2026-04-25 in `2026-04-25T13-36-00Z_skill-set-cycle/iter_03` (five_hour rejected, sleep 6811.5s matched parsed reset within ±60s, retry session `14f48ecd` resumed cleanly at the wake_at, full chain completed, MANIFEST.rate_limit_pauses[0] records the full timeline). Item (f) Telegram fired both pause/resume notifications still requires a `sst-chain-driver`-wrapped run (today's iter_03 went directly through `bin/skill-chain.py`, bypassing `bin/drive-chain.py`'s Telegram event watcher).
- Phase 12 `<persona>-chain-driver proprietary counterpart` — reason: spec Phase 12 #5 tail (renamed in Phase 15); the transferable `sst-chain-driver` (formerly `sst-orchestrator`) landed but its proprietary counterpart (configured `watched-chain` / `default-loop` / `default-max-budget-usd` / `default-max-cycles` / `telegram-env` / `label` block, mirroring `sst-manager` / `<persona>-manager`) was deferred. First proprietary lands in a consuming project's `.claude/skills/<persona>-chain-driver/SKILL.md` + a `<persona>-cycle-driver.yaml` chain entry if needed. Until this lands the transferable can still be invoked with all caps on the CLI.
- Phase 14 `Acceptance check: kill -TERM mid-supervisor + verify self-heal` — reason: spec Phase 14 close item; one combined acceptance test for the four Phase 14 mechanisms above.
- Phase 12 `sst-supervisor fast-path on clean` — reason: spec Phase 12 efficiency-win 2; ~5 of 9 sdrai cycles produced no findings yet supervisor still spent $0.97–1.45 each. §0.5 keyword-scan pre-check + clean-deploy verification short-circuits to a one-line verdict; never applies when prior verdict says escalate.
- `sst-dev-cycle` description frontmatter says "in one amended commit" — reason: stale prose, contradicts §5/§6 of the body which both forbid `--amend` and document why the Just-shipped line omits SHAs. Fix: remove the word "amended" so description reads "in one commit". Trivial prose nit; bundle with any other small `sst-dev-cycle` prose touch-up.
- Phase 12 `Acceptance: ≥25% cost reduction on multi-iteration runs vs Phase 11 baseline` — reason: spec Phase 12 close item; a real sdrai-cycle with all four wins live, measured against the $73.59 / 9-cycle baseline. Now invocable via `bin/skill-chain.py --chain dev-cycle-with-review-looped` (shipped this cycle).
- Phase 8.6 end-to-end smoke: chain `sst-web-research → sst-editorial-pass → sst-social-promoter` against a real consuming project with clean supervisor verdict — reason: spec Phase 8 tail item, Phase 8.1-8.5 lifts already complete.
- Phase 2 `User runs bin/install-skills.sh -y to deploy the updated sst-dev-cycle/sst-dev-review into ~/.claude/skills/` — reason: user action, still pending.
