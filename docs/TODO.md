# skill-set TODO (handoff doc)

> Cross-cycle state. Every skill reads this on start and updates it on close. Three sections, in this order. Primary spec: `docs/SPEC.md`.

## In flight

<!--
  Exactly one line per currently-running skill, format:
  - [<skill-name> @ <utc-iso>] <one-line: what this skill is currently doing>
  Rewrite (don't append) as the focus narrows. Empty when no skill is running.
-->


## Just shipped (last cycle)

<!--
  Append-on-close, newest first. Format:
  - <one-line summary> — by <skill-name> at <utc-iso>
  No commit SHA: a commit cannot contain its own hash, and amend-based
  workarounds produce stale references. Correlate entries to commits via
  `git log --oneline --grep '<keyword>'`. Older entries below retain their
  SHAs from the prior two-commit pattern; leave them alone, they're valid.
  Trim to the most recent 10 entries; older history lives in docs/SPEC.md
  phase blocks and `git log`.
-->

- [easy] [fix] `bin/drive-chain.py`: added `_is_worker_stale()` helper (compares `manager-bot.py` mtime vs worker pane's `/proc/<pid>/stat` start-time) and recycle logic in `main()` — if stale, kill the existing tmux session before starting a fresh one, so chain-driver upgrades are picked up on the next run. — by skill-set-dev at 2026-04-29T12:04:18Z
- [medium] [should-fix] `bin/skill-chain.py` flat-run MANIFEST: promoted `difficulty`, `difficulty_source`, and `rate_limit_pauses` from `iterations_collected[0]` into the session manifest when `not looping`, enabling §2.11 batch-sizing and supervisor §3.5.1 refinement on debug-mode single-iter runs. — by skill-set-dev at 2026-04-29T12:04:18Z
- [easy] [fix] `sst-dev-review` v1.4.6 → v1.4.7 + `sst-dev-cycle` v1.4.1 → v1.4.2 + proprietary mirrors: corrected §2.10/§2.11 to measure dev-skill tokens only (skills[0], not full-chain sum); reverted band edges to dev-skill targets (easy 100-200k, medium 200-300k, hard 400-500k); updated operating-principle parenthetical in dev skills to state the review fires on the dev's own number. Inline sanitize: must-fix=0, should-fix=0, nit=0. Validator clean (25 skills + 7 chains; 5 proprietary + 2 chains). — by skill-set-dev at 2026-04-29T11:35:23Z
- Phase 22 [easy] [should-fix] `sst-dev-review` v1.4.5 → v1.4.6 + `skill-set-dev-review` v1.2.5 → v1.2.6: raised §2.10/§2.11 full-chain band uppers for medium (300k → 430k) and hard (500k → 630k) to accommodate dev-skill's own window target + ~130k review+supervisor overhead; §176/§191 header prose updated to name all three dev-only targets (easy 100-200k, medium 200-300k, hard 400-500k). Inline sanitize: must-fix=0, should-fix=0, nit=0. Validator clean (25 skills + 7 chains; 5 proprietary + 2 chains). — by skill-set-dev at 2026-04-29T07:42:37Z
- Phase 23 [hard]: authored new transferable `sst-wiki-curator` v1.0.0 under `skills/research/` for building and maintaining LLM-curated knowledge wikis (3 variants: minimal | middle | scripted; 2 modes: scaffold + ingest/maintain). 470-line SKILL.md covers project contract, three-layer architecture (raw → wiki → schema spec), file conventions, per-variant workflows, license tracking, anti-patterns, scripts reference. Inline sanitize: must-fix=0, should-fix=0, nit=0 (Karpathy gist URL retained as public technical citation per sst-web-research convention; Claude Code retained per `templates/sanitization-guidance.md` §Acceptable references; no project subdir names or `~/Dev/<domain>/` paths). Validator clean (25 skills + 7 chains). — by skill-set-dev at 2026-04-29T07:11:58Z
- [easy] pushed /feedback to BotFather command list via Telegram setMyCommands API; all 9 commands confirmed (status, objectives, proposals, promote, feedback, pause, resume, ping, help) — by skill-set-dev at 2026-04-29T01:17:18Z
- [medium] sst-manager v1.3.0→v1.4.0: rewrote §4 digest format — 5-section structured template (what shipped: N commits + ≈$spend + commit subjects; what stalled; goals with evidence; open queue count+top; pending review count); dropped 3 colloquial examples; replaced with 2 concrete examples showing commit subjects + plain-English explanations; language rules kept + "keep technical specifics" bullet added. Inline sanitize: must-fix=0, should-fix=0, nit=0. Validator clean. — by skill-set-dev at 2026-04-28T13:23:41Z
- [easy] bin/manager-bot.py + README: added worker-lifecycle note to /help reply ("Replies are live only during chain runs; commands sent between runs queue and ack on the next session start.") + matching paragraph in README → "Telegram bot" → "Daily commands". — by skill-set-dev at 2026-04-28T13:23:41Z
- Phase 2 [easy]: deployed all 24 transferable skills to `~/.claude/skills/` via `bin/install-skills.sh --force`; 5 stale-diverged force-updated (sst-dev-cycle v1.3.1→v1.4.1, sst-dev-review v1.3.1→v1.4.5, sst-supervisor v1.6.1→v1.8.2, sst-chain-driver v1.2.0→v1.2.1, sst-manager); 8 updated, 5 new (sst-setup-telegram, sst-lead-generation, sst-linkedin-easy-apply, sst-linkedin-networking, sst-social-promoter). Validator clean. — by skill-set-dev at 2026-04-28T08:21:11Z
- Phase 22 [easy] [should-fix] `sst-dev-cycle` v1.4.0 → v1.4.1 + `sst-dev-review` v1.4.4 → v1.4.5 + mirrors: documented dev-only vs. full-chain distinction in window targets across 4 skills; raised review easy cap 250k → 350k to accommodate full dev budget (dev 200k + review 50k + supervisor 80k ≈ 330k). Inline sanitize: must-fix=0, should-fix=0, nit=0. Validator clean (24 skills + 7 chains; 5 proprietary skills + 2 chains). — by skill-set-dev at 2026-04-28T04:46:18Z

## Next up (queued for next cycle)

<!--
  One line per queued item. The next cycle picks the top item unless the spec says otherwise.
  Format:
  - <one-line description> — <reason/source: spec phase X.Y, supervisor verdict <sha>, manager directive, user message>
  Order: blockers/highest-impact first.
-->

- [hard] [refactor] Replace the dumb verbatim-prepend feedback path with on-demand manager-agent processing. **Problem.** Today: bot writes queue file → manager (cron OR pre-hook) prepends user body verbatim → supervisor reads next iter and tries to interpret. The manager has more context than the supervisor (objectives.md, full SPEC/TODO, multi-project view), but that context goes unused; the supervisor only has run-log context, so vague feedback often goes nowhere. **New path.** (1) Bot writes the durable queue file (crash safety), then immediately spawns `claude --print "/skill-set-manager --process-feedback <queue-file>"` out-of-band — no waiting on cron or chain-runner. (2) Manager, in on-demand mode, reads the feedback body + `objectives.md` + `docs/SPEC.md` + `docs/TODO.md` + the most recent run log, then *decides* where to land it: **(a)** Direct queue item — feedback maps to a queueable work item fitting objectives → append to `docs/TODO.md > Next up`; **(b)** SPEC addition — feedback is bigger than one cycle → append to `docs/SPEC.md`; **(c)** Soft steering — feedback is shape-ish, not discrete → write a structured directive to `manager-notes.md` (`## <utc> manager-translated user feedback` heading, body names what the user said + what objectives apply + what action the manager recommends to the supervisor); **(d)** Refuse / clarify — anti-fork conflict → refuse via Telegram, or ambiguous → ask via Telegram. (3) Manager always replies to the user via Telegram with what it decided and where the change landed (file path + section). **Hard-rule expansion.** `sst-manager`'s "read-only across watched projects" rule gets a scoped exception for `docs/TODO.md > Next up` appends and `docs/SPEC.md` appends, modeled on the existing `objectives.md` `[ ]` ↔ `[x]` exception. Document precisely: appends only; never edits or deletes; never touches `In flight` or `Just shipped`; never modifies SPEC phase headings; never commits (git changes still belong to the dev cycle on its next pass). **Two-file collapse rolls in here.** Collapse `manager-feedback.md` + `manager-guidance.md` into `manager-notes.md` with source-tagged headings (`## <utc> user feedback (chat <id>)` for verbatim, `## <utc> manager-translated user feedback` for on-demand-routed, `## <utc> manager observation` for cron-derived patterns). Single ~3KB cap. Conflict resolution: user feedback > manager-translated > manager observation; newer beats older within source. Migration: on first run, helper merges legacy files into `manager-notes.md` (interleaved by UTC, source-tagged by origin) and renames legacy to `~/.claude/state/.archive/<name>.<utc>.md`. **Supervisor reads.** Update `sst-supervisor.SKILL.md` §Inputs: split into "run-log inputs" (items 1-3) and "cross-run state inputs (always read at session start, even if absent)" — drop "if it exists" guards, replace with "Read; if absent, note 'no manager notes present' in verdict." Add §0.4 (before §0.5 fast-path) that does cross-run state reads explicitly. Update `skill-set-supervisor.SKILL.md` `## Locations` to list `manager-notes.md`. Verdict §6 must name which `manager-notes.md` entries (by `## <utc>` heading) were applied this iter; missing line = §6 contract violation. **Queue-file role narrows.** Durability-only buffer for crash recovery; cron tick + chain-runner pre-hook are degraded fallbacks. **Worked example.** User's 2026-04-29 feedback (*"don't install all skills, only skills already installed may be updated"*): smart manager reads it, finds Phase 2 install items in TODO + SPEC, recognizes a concrete behavioral change to `bin/install-skills.sh`, lands outcome (a) — appends to `Next up`: *"[medium] [fix] `bin/install-skills.sh` defaults to update-only; new skills require explicit `--install <name>`. User directive 2026-04-29."* — and replies via Telegram: *"Routed as new TODO item under Next up; will be picked up by next dev cycle."* **Acceptance.** (i) `/feedback <concrete change>` mid-run → new `Next up` item in `docs/TODO.md` within ~60s + Telegram reply names file/section; (ii) `/feedback <shape-ish>` → entry in `manager-notes.md` with manager reasoning paragraph; (iii) `/feedback "skip sanitize on next transferable write"` → refuse-with-explanation Telegram reply, no file changes; (iv) `/feedback <ambiguous>` → clarifying question Telegram reply, queue file stays unprocessed; (v) crash on-demand spawn → next manager cron tick or chain-runner pre-hook processes queue file as fallback, idempotent via `<!-- src: <basename> -->` marker. **Reason:** user-flagged 2026-04-29 — verbatim routing wastes the manager's contextual intelligence; the manager IS the smart loop, not a passthrough. Subsumes the unified-notes refactor and the supervisor-skip-fix items.
- [medium] [fix] Feedback fallback path: chain-runner pre-iter drain. Post-refactor (item above), the happy path is bot → on-demand manager spawn → smart routing in ~30s. This item covers crash recovery: if the on-demand spawn fails (rate-limited, harness crash, system reboot, network glitch), the queue file persists. Without a fallback, feedback sits unrouted indefinitely. Fix: `bin/skill-chain.py` calls `bin/manager-write-state.py --drain-feedback-queue` (helper item below) at the start of every iter — at minimum prepends a verbatim `## <utc> user feedback (chat <id>)` entry to `manager-notes.md` so the supervisor sees it on the next iter (degraded smart routing, but not lost). Ideally the fallback also kicks off a fresh on-demand manager spawn to do the smart pass; if that spawn ALSO fails, the verbatim prepend is the floor. Manager's 08:00/20:00 cron tick is a slower fallback (also calls the helper). Correctness requirements (the helper enforces these so callers don't coordinate): (1) **No misses across stacking** — process all queued files in received-at order; prepend oldest first so newest ends up at the top of `manager-notes.md`. (2) **No duplicates across crash** — for each file, prepend (with fsync) BEFORE renaming queue file to `manager-bot-queue/processed/<name>.json`; rename(2) atomic on the same FS. Belt-and-suspenders: embed `<!-- src: <basename> -->` in the entry; subsequent drains skip any queue file whose basename already appears in `manager-notes.md`. (3) **No duplicates across concurrent drainers** — exclusive `flock(2)` on `~/.claude/state/manager-bot-queue/.drain.lock` for the full glob+process+rename sequence; second drainer waits, finds queue empty. Acceptance: (a) `/feedback test` mid-run with on-demand spawn disabled → `manager-notes.md` updates within seconds, next supervisor iter reads it; (b) stack 3 `/feedback` messages → all 3 appear newest-first, no duplicates; (c) `kill -9` the helper after fsync but before rename → entry NOT re-prepended on next drain; (d) manager cron tick + chain-runner pre-hook fired simultaneously against 5-message queue → exactly 5 entries land. Reason: depends on the helper landing first AND the on-demand refactor.
- [medium] [fix] Manager state writes to `~/.claude/state/manager-notes.md` (post-refactor) currently rely on the Write tool, which trips an interactive permission prompt even with `Write(*)` in global settings — the harness applies extra caution to writes outside `cwd`. That breaks the cron-driven `claude --print` invocation path the manager is supposed to run under. Move all writes behind a thin helper: add `bin/manager-write-state.py` (pure-Python, stdlib only). Three modes (post-refactor; single target file `manager-notes.md`): (a) `--source feedback --src-file <queue.json>` reads one queue file, prepends one user-feedback entry (heading: `## <utc-iso> user feedback (chat <id>)`), renames the queue file to `manager-bot-queue/processed/<name>.json`; (b) `--source observation` reads the new entry from stdin, prepends with heading `## <utc-iso> manager observation`; (c) `--drain-feedback-queue` globs `manager-bot-queue/*_feedback.json`, sorts by received-at, calls mode (a) on each — this is what the chain-runner pre-hook calls AND what the manager skill's §1 calls. All modes: write atomically (tempfile + fsync + rename), preserve leading H1 + lead paragraph, prepend new entry just under them, trim oldest entries from the bottom (single ~3KB cap regardless of source). On first invocation: detect legacy `manager-feedback.md` and/or `manager-guidance.md`, merge entries into `manager-notes.md` (interleaved by UTC, source-tagged by origin file), rename the legacy files to `~/.claude/state/.archive/<name>.<utc-iso>.md`; subsequent invocations see only `manager-notes.md`. Mode (c) holds an exclusive `flock(2)` on `manager-bot-queue/.drain.lock` for the full glob+process+rename sequence (concurrent drainer = second one waits + finds queue empty). Mode (a) embeds the queue file's basename as `<!-- src: <basename> -->` in the prepended entry; mode (c) skips any queue file whose basename already appears in `manager-notes.md` (idempotency belt against "prepend committed, rename failed"). Then update `sst-manager.SKILL.md` to direct the manager to invoke the helper via Bash for both source kinds (collapsed §1 + §5 routing into one helper call pattern); mirror any duplicated prose in the proprietary `skill-set-manager`. Add `Bash(/home/rob/Dev/skill-set/bin/manager-write-state.py:*)` to `~/.claude/settings.json` allowlist. Side benefit: the prepend+trim+correctness+source-tagging logic stops being re-implemented from prose on every invocation, and the chain-runner pre-hook (next item) is one line: `subprocess.run([helper, "--drain-feedback-queue"])`. Reason: user-reported 2026-04-29 — the Write tool prompts on these paths from auto-mode, blocking unattended cron runs of `/skill-set-manager`. Lands AFTER the unified-notes refactor (item 1 above) and BEFORE the chain-runner pre-hook (item 2 above; helper is the dependency).
- [medium] Phase 23 acceptance: invoke `/sst-wiki-curator scaffold <test-path> --variant minimal` against a throwaway dir and confirm directory tree, schema-spec self-containment, `INIT` log line, git-init + first commit; then `/sst-wiki-curator ingest <test-path> <test-source-md>` and confirm raw drop, paper page with full front matter, topic page(s) created/updated, `index.md` updated, `INGEST` log line, one-commit close. Reason: spec Phase 23 acceptance item.
- [easy] Phase 23 install: run `bin/install-skills.sh -y` so `sst-wiki-curator` becomes harness-discoverable for the Phase 23 acceptance test. Reason: spec Phase 23 install item.
- [medium] Phase 18 `acceptance check on chain-bound worker lifecycle`. Reason: spec Phase 18 item 5; the implementation landed this cycle (item 4 `[x]`) but acceptance requires a real `/skill-set-chain-driver` round-trip with Telegram. Confirm: (a) chain driver starts the `skill-set-bot` tmux session at session-start and the user can `/ping → pong` during the run; (b) chain driver kills the tmux session at session-end; (c) re-run with the worker pre-started by hand (e.g. `tmux new-session -d -s skill-set-bot ...`): chain driver detects the pre-existing worker, does NOT start a second one, does NOT kill it at session-end; (d) two simultaneous chain-driver runs against different chains: only one tries to start the worker (flock); (e) manager invocation while no chain is running succeeds with an empty inbound queue and a digest body that does NOT re-notify any currently-paused job.
- [easy] Phase 17 `acceptance check on empty-queue bail`. Reason: spec Phase 17 item 4; the steady-state run requires `Next up` empty AND every SPEC `[ ]` flipped `[x]` (this cycle didn't reach that state, so item 4 stayed `[ ]`). When the queue eventually empties, invoke `/skill-set-chain-driver` and confirm: (a) `skill-set-dev` exits 0 with `[no-work] ...` printed and no commit; (b) chain runner aborts the loop after the first empty iter (no second iter starts; `MANIFEST.loop.terminated_by == "no_work_bail"`); (c) iter MANIFEST records `no_work_bail` with the sentinel line; (d) cumulative cost is bounded by the dev skill's pre-flight reads (~$0.10-0.30) rather than a full ~$8 iter; (e) chain-driver session-end Telegram body labels the stop "no-work bail".
- [medium] Phase 14 `Acceptance check: kill -TERM mid-supervisor + verify self-heal`. Reason: spec Phase 14 close item; one combined acceptance test for the four Phase 14 mechanisms above.
- [medium] Phase 20 `goose-cerebras harness` (deferred). Reason: spec Phase 20 (deferred behind Phase 19); the per-skill model-tier work in Phase 19 delivers the bulk of the 24/7-productivity throughput win within the existing Max subscription, so this becomes a future supplement for free-tier capacity rather than the primary fix. Re-pick when Phase 19 acceptance ships and a real cost / throughput baseline shows Phase 19's win is insufficient AND mechanical-skill work has been cleanly identified for offload to Cerebras. Architecture as in spec: `GooseCerebrasHarness` subclass + `bin/goose-shim.py` translating Goose's stream-json event vocabulary to Claude Code's shape + per-skill `allowed-harnesses:` anti-fork guard.
- [medium] Phase 8.6 end-to-end smoke: chain `sst-web-research → sst-editorial-pass → sst-social-promoter` against a real consuming project with clean supervisor verdict. Reason: spec Phase 8 tail item, Phase 8.1-8.5 lifts already complete.
