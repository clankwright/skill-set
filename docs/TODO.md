# skill-set TODO (handoff doc)

> Cross-cycle state. Every skill reads this on start and updates it on close. Three sections, in this order. Primary spec: `docs/SPEC.md`.

## In flight

<!--
  Exactly one line per currently-running skill, format:
  - [<skill-name> @ <utc-iso>] <one-line: what this skill is currently doing>
  Rewrite (don't append) as the focus narrows. Empty when no skill is running.
-->



## Just shipped (last cycle)

<!--
  Append-on-close, newest first. Format:
  - <one-line summary> — by <skill-name> at <utc-iso>
  No commit SHA: a commit cannot contain its own hash, and amend-based
  workarounds produce stale references. Correlate entries to commits via
  `git log --oneline --grep '<keyword>'`. Older entries below retain their
  SHAs from the prior two-commit pattern; leave them alone, they're valid.
  Trim to the most recent 10 entries; older history lives in docs/SPEC.md
  phase blocks and `git log`.
-->

- Phase 19 #8 docs landed: new top-level `## Model-tier routing` section in `README.md` sits between `## Chain YAML fields` (and its sub-sections, including Rate-limit handling) and `## Telegram bot`, documenting the full Phase 19 routing surface — per-skill `model-floor:` + `effort-floor:` frontmatter fields with their default values (`opus` / `high`); the framework's canonical floor table grouped by skill class (`sst-supervisor` + `sst-sanitize-transferable` at Opus+xhigh; `sst-dev-cycle`/`sst-dev-review`/`sst-skill-router`/`sst-editorial-pass`/`sst-iterative-writer`/`sst-literary-critic` at Sonnet+high; `sst-translator`/`sst-fact-checker`/`sst-promote-skill-proposal`/`sst-output-selector`/`sst-llm-judge-ranker`/`sst-email-control-loop`/`sst-setup-telegram` at Haiku+medium); the per-item difficulty label format on SPEC items (`- [ ] [<difficulty>] <description>`) and TODO Next-up entries (`- [<difficulty>] <description>. Reason: ...`); the resolution rule `effective_model = max(item.model_tier, skill.model_floor)` over `{haiku < sonnet < opus}` and `effective_effort = max(item.effort_tier, skill.effort_floor)` over `{low < medium < high < xhigh < max}` resolved independently per axis; the runner's pre-parse → `[picked-difficulty: <tier>]` sentinel capture → dev-only override flow with the `[route] /<skill>: difficulty=<d> floors=(<m>,<e>) -> model=<M> effort=<E>` log format and the `route` sub-record landing on the iter manifest; a worked example tracing a `[medium]` item through `sst-dev-cycle → sst-dev-review → sst-supervisor` showing the dev + review skills resolve to Sonnet+high (effort floor wins on the effort axis; model floor matches item tier on the model axis) while the supervisor still resolves to Opus+xhigh (both floors win on both axes regardless of item difficulty); the throughput-impact paragraph (`~25-35% of an all-Opus+xhigh baseline`, `~3-4× more iters per Max window`, supervisor + sanitize stay on Opus+xhigh on every cycle since their floors are absolute, auto-promoted transferable rewrites are still authored at the supervisor's Opus+xhigh route); and the anti-fork rule binding at both axes (no fifth resolution input that bypasses floors; the `max()` rule is the contract). CLAUDE.md "Choosing a chain" gains a closing "Throughput note (Phase 19 routing)" paragraph after the "It is safe to invoke any of these even when ..." paragraph, summarizing the ~2-4× throughput lift on dev/review skills while flagging that the supervisor + sanitize-transferable still consume Opus+xhigh quota at the old rate (with the `max(any_item_tier, opus) = opus` and `max(any_item_effort, xhigh) = xhigh` arithmetic explicit), pointing to the README's Model-tier routing section for the floor table + worked example, and noting that the proprietary `skill-set-supervisor` (project-scoped) is the auto-appended supervisor in this repo and carries the canonical Opus+xhigh floors directly, while consuming projects pick up the canonical `sst-supervisor` floors on the next `bin/install-skills.sh -y --force` (queued in `Next up` as the long-stale Phase 2 install follow-up). No skill or chain YAML touched (changes confined to `README.md` + `CLAUDE.md` + `docs/SPEC.md` + `docs/TODO.md`), no transferable SKILL.md touched, so the sanitization gate is skipped (README + CLAUDE.md are project-scoped operator-facing prose, not transferable skill bodies). Validator clean (24 skills + 7 chains; the auto-walk covers `skills/` and `chains/`, which were untouched). SPEC Phase 19 item #8 (line 324) flipped `[ ]` → `[x]` with the closure paragraph documenting the README section structure + CLAUDE.md insertion point + sanitization-gate skip rationale. Phase 19 still has two open `[ ]` items remaining: #7 (`[hard]` tag every transferable + proprietary SKILL.md with `model-floor:` + `effort-floor:` — a multi-skill commit gated on a sanitize judgment pass per transferable; queued for a dedicated cycle since #7's blast radius dwarfs #8's docs-only scope) and #9 (`[hard]` acceptance with mixed-difficulty items across a `--loop 3` run — gated on Phase 17/18 acceptance prerequisites which themselves require live Telegram chain runs already queued in Next up). (by skill-set-dev at 2026-04-27T08:57:06Z)
- Phase 19 #6 sst-dev-review §4 prose closed: every Review-follow-up entry the skill files now requires a leading `[<difficulty>]` bracket sitting before the existing `[<severity>]` bracket on BOTH the spec surface (`- [ ] [<difficulty>] [blocker|should-fix] ...`) and the `TODO.md > ## Next up` mirror (`- [<difficulty>] [blocker|should-fix] ...`). Format examples in §4 updated end-to-end; rules list extended with "Every entry MUST carry a difficulty label as the leading bracket immediately after the checkbox, with the severity bracket second" + "Difficulty is independent of severity and does not affect ordering" so the existing severity-then-file-line ordering is preserved (severity governs priority, difficulty governs routing). New "Assigning difficulty from the finding's nature" mid-section paragraph codifies the heuristic the review skill uses when filing the bracket: difficulty answers "how much reasoning does the FIX cost?" — NOT "how serious is the BUG?" — since severity already covers seriousness. Default mapping made explicit: `[easy]` covers prose nits / single-line typos / heredoc hoists / YAML scalar quoting / known-good migrations to N call-sites / frontmatter tagging with values the spec already names; `[medium]` covers bounded code change touching one module + its tests / localized helper rewrite / softening one halt-condition to note-and-proceed with a narrow exception / contract addition the spec has already designed (no new schema decisions); `[hard]` covers cross-file refactor / new schema field with runner support / concurrency or lifecycle invariants (refcount, flock, signal handling) / fresh design judgment / security-or-data-integrity surface interaction. Cross-tier tie-breaker explicit ("if the fix straddles two tiers, pick the higher one — under-routing burns the cycle on a too-small model; over-routing only spends quota"); orthogonality with severity demonstrated by two worked counter-cases (a `[blocker]` that's mechanically a one-liner stays `[easy]`; a `[should-fix]` that needs a refcount stays `[hard]`). The TODO.md format example explicitly notes the same `<difficulty>` token must be reused on both surfaces so spec-and-TODO entries stay in lockstep. Frontmatter `version: 1.2.1 → 1.3.0` (minor bump for the new contract requirement; the field-shape addition is backward-compatible at the prose level since unlabeled entries still parse and route via the runner's existing `*-unlabeled → medium` graceful-degradation path landed in Phase 19 #4 + #5, but the skill-side authoring contract now obligates the label going forward). Inline sanitize judgment pass: the only new tokens added are framework-canonical labels (`[easy]`/`[medium]`/`[hard]`/`[blocker]`/`[should-fix]`), generic resolution-rule pseudocode (`effective = max(item_tier, skill_floor)` per axis), and a reference to `templates/SPEC.md`'s "Difficulty labels" appendix; no project nouns, no banned-list terms (toadlyBroodle / landonmutch / sdrai / csvagent / botlab / satring / sn-bot / akiya / mpp / lnbits / phoenixd / rc.d / gunicorn all absent), no internal phase numbers in transferable prose. Verdict: must-fix=0, should-fix=0, nit=0. Validator clean (24 skills + 7 chains; sst-dev-review SKILL.md is the only file in the auto-walk that changed). Closes Phase 19 sub-item #6 in the same cycle that already shipped the dev-side sentinel-emit (item #7); the queue now requires labels on both authoring paths — the dev's pick at iter start AND the review's findings at iter end. Two Phase 19 review-side sub-items remain open: #7 (tag every transferable + proprietary SKILL.md with `model-floor:` + `effort-floor:` floors), #8 (README + CLAUDE.md "Model-tier routing" docs). (by skill-set-dev at 2026-04-27T08:37:29Z)
- `bin/notify-telegram.sh` now auto-sources `$TELEGRAM_ENV_FILE` when set AND `TELEGRAM_BOT_TOKEN` is not already exported, closing the queued Next-up item 6 (`sst-setup-telegram` §5 prose vs helper interface mismatch caught during the 2026-04-26 skill-set bot setup). Picked option (b) per the entry's recommendation: every consumer of the helper benefits, and the skill prose at `skills/framework/coms/sst-setup-telegram/SKILL.md:148-155` (which already documented `TELEGRAM_ENV_FILE=<env-path> bash <repo>/bin/notify-telegram.sh` as the preferred test invocation) becomes accurate WITHOUT requiring a transferable edit + sanitize-gate round, so the change ships outside the `skills/<category>/<sst-*>/SKILL.md` sanitize scope. New behavior in `bin/notify-telegram.sh`: an early block at lines 28-34 — `if [ -n "${TELEGRAM_ENV_FILE:-}" ] && [ -z "${TELEGRAM_BOT_TOKEN:-}" ]` — sources the file via `set -a; . "$TELEGRAM_ENV_FILE"; set +a` (the standard "export every variable assigned during the source" idiom), but ONLY when the caller has not already exported the token. The `[ -z TELEGRAM_BOT_TOKEN ]` guard preserves the chain driver's `tg_env.setdefault` precedence rule (explicit shell env wins; the env-file value never silently overwrites a deliberate caller-set token); without the guard, a tmux-pane-pre-sourced token could be replaced by whatever a stale env file holds. Missing/unreadable `$TELEGRAM_ENV_FILE` errors fast with `notify-telegram: TELEGRAM_ENV_FILE=<path> not readable` to stderr + exit 1, BEFORE the required-env check fires, so the diagnostic clearly points at the `$TELEGRAM_ENV_FILE` value rather than the post-source `TELEGRAM_BOT_TOKEN is required` message that previously made the symptom feel like a missing-shell-var bug. The required-env error messages also now mention the env-file alternative (`TELEGRAM_BOT_TOKEN is required (or set TELEGRAM_ENV_FILE to a .env file containing it)`) so a future operator who hits the legacy required-env path discovers the auto-source affordance from the error itself. Header comment block extended with a `TELEGRAM_ENV_FILE` entry under "Optional env" documenting the auto-source semantics + precedence rule. Smoke-tested four cases: (1) `TELEGRAM_ENV_FILE=<tmpfile> bash bin/notify-telegram.sh` with token+chat in the file, no shell env → past the env-required check, hits Telegram API with the fake token (404 expected from the bogus token, proving the source step worked); (2) `TELEGRAM_ENV_FILE=/nonexistent/path` → exit 1 with the readable-error stderr; (3) no `TELEGRAM_ENV_FILE` and no shell env → exit with the required-env error mentioning the env-file alternative; (4) shell `TELEGRAM_BOT_TOKEN` exported AND `TELEGRAM_ENV_FILE` set to a file with a different token → auto-source skipped, shell value preserved (verified via the same guard logic in a one-line bash subshell). `bash -n bin/notify-telegram.sh` clean. CLAUDE.md "Telegram bot" outbound bullet rewritten to document both invocation paths (the new auto-source `TELEGRAM_ENV_FILE=<env-path> bash bin/notify-telegram.sh` form alongside the legacy `( set -a; . <env-path>; set +a; ... )` pre-source form), with explicit precedence note that explicit shell env wins. SPEC Phase 18's "Closed review follow-ups" trailer extended one entry. No skill or chain YAML touched (changes are in `bin/notify-telegram.sh` + `CLAUDE.md` + `docs/SPEC.md` + `docs/TODO.md`); `skills/framework/coms/sst-setup-telegram/SKILL.md` intentionally NOT edited since its existing §5 prose now matches the helper's behavior — the entire point of picking option (b) was the no-skill-edit pathway. Validator clean (24 skills + 7 chains; bin/ change doesn't affect the SKILL.md/chain YAML walk). No transferable touched, so the sanitization gate is skipped. (by skill-set-dev at 2026-04-26T16:31:46Z)
- Phase 19 (4)+(5) runner-side model + effort resolution closed end-to-end. Three pieces in one commit per the Phase 19 (4)+(5) runner-changes carveout grouping. (a) `bin/skill-chain.py`: `Harness.build_command` signature gains keyword-only `model=` + `effort=` (subclasses must accept them; `ClaudeCodeHarness` passes `--model {model or DEFAULT_MODEL_FLOOR}` + `--effort {effort or DEFAULT_EFFORT_FLOOR}` so direct ad-hoc invocations without the routing context still produce a working command). Threading: `run_iteration` resolves per-skill route via new `_resolve_skill_route(skill, iter_difficulty, cwd)` → `_resolve_skill_route` reads frontmatter via `_find_skill_md` (project-scoped first, personal-global second; mirrors harness lookup) + `_read_skill_frontmatter` (best-effort YAML parse, empty-dict on any failure so a missing/malformed SKILL.md never aborts a cycle), then computes `effective_model = _max_tier(item_model, skill_model_floor, MODEL_TIERS)` and `effective_effort = _max_tier(item_effort, skill_effort_floor, EFFORT_TIERS)` independently. `_max_tier` falls back to the highest ordering element when both inputs are unknown so junk never silently selects too-cheap a tier. Defaults: `model_floor=opus`, `effort_floor=high` (matches the schema's documented enum defaults). Each skill's iteration manifest entry gains a `route` sub-record (difficulty + floors + item tiers + effective tiers) for post-hoc analysis; per-skill `[route] /<skill>: difficulty=<d> floors=(<m>,<e>) -> model=<M> effort=<E>` line logged before the skill banner. (b) Difficulty pre-parse: `_resolve_iter_difficulty(cwd)` runs at iter start, returning `(tier, source)` over five source codes (`todo-next-up`, `todo-next-up-unlabeled`, `spec-first-open`, `spec-first-open-unlabeled`, `no-source`). Reads `docs/TODO.md > Next up` first list item's `[easy|medium|hard]` bracket (HTML comments stripped before scan, header bounded by next `^##\s+`); falls back to `docs/SPEC.md` first open `- [ ]` item's bracket only when Next up is missing/empty. Unlabeled-and-no-source cases default to `medium` with an ORANGE warn on stderr; labeled cases log a DIM line. Lands on `iter_manifest.difficulty` + `iter_manifest.difficulty_source`. (c) Sentinel capture: `PICKED_DIFFICULTY_SENTINEL_RE` (line-anchored `^\s*\[picked-difficulty:\s*(easy|medium|hard)\]\s*$`, multiline + case-insensitive, mirror of Phase 17's `NO_WORK_SENTINEL_RE`) captured in `handle_event` from assistant-text-only blocks, first-match-wins per skill, stashed on `skill_record.picked_difficulty`. After the FIRST iter skill exits (`i == 0` — the dev), if `picked != iter_manifest.difficulty`, override `iter_manifest.difficulty` for downstream skills (review, supervisor) and stash the original on `iter_manifest.difficulty_dev_picked`; tool inputs/results never trigger because only `block.type == "text"` reaches the scanner. The override is one-way per Phase 17's discipline (review/supervisor echoing the bracket inline can't shift routing for the same iter's downstream pass — there is no downstream after supervisor anyway). 30+ inline assertions cover `_max_tier` happy + edge + junk-input fallback (8 cases), `_read_skill_frontmatter` hits + None + missing path (5), `_find_skill_md` project-scoped wins + personal-global-only fallback + nonexistent-None (3), `_resolve_iter_difficulty` over labeled/unlabeled Next-up + SPEC fallback + no-source (4 temp-dir cases plus the live repo case), `_resolve_skill_route` matrix incl. unknown-skill default fallback (4), sentinel regex line-anchor + case-insensitive + inline non-match (5), and `build_command` model/effort threading + default-fallback (4). (d) Floor tagging on the framework's safety-critical skills, the gate that closes Phase 19's open carveout review-follow-up without an intervening carveout commit: transferable `skills/framework/sst-supervisor/SKILL.md` v1.6.0 → v1.6.1 + `skills/framework/sst-sanitize-transferable/SKILL.md` v1.0.0 → v1.0.1 both gain `model-floor: opus` + `effort-floor: xhigh`; proprietary `.claude/skills/skill-set-supervisor/SKILL.md` v1.0.0 → v1.0.1 same (applied via `bin/apply-skill-patch.py` per skill §3 permissions-pivot for `.claude/skills/**` paths; this proprietary file is local-only since `.claude/` is gitignored, so the change rides the cycle as a working-tree-only tweak rather than as a committed file). With (4) live, the per-skill effort-floor resolution is the source of truth; supervisor + sanitize floors win regardless of item difficulty (`max(any_item_effort, xhigh) = xhigh`), so the carveout never needed to land. Inline sanitize judgment on the two transferable touches: must-fix=0, should-fix=0, nit=0 (only schema-canonical enum values `opus` + `xhigh` + a SemVer bump; no project nouns, no banned-list terms, no internal phase numbers, no proprietary paths). `bin/install-skills.sh -y` propagated the canonical sanitize-transferable update to its harness mirror; sst-supervisor + sst-dev-cycle + sst-dev-review remain DIVERGED at the harness-mirror layer (long-queued Phase 2 install follow-up), so the canonical floors only take effect for consuming projects after a `bin/install-skills.sh -y --force` — in THIS repo the proprietary `skill-set-supervisor` (project-scoped, just-tagged) is the auto-appended supervisor and routing already applies xhigh here. SPEC Phase 19 items #4 + #5 flipped `[ ]` → `[x]` with closure paragraphs documenting the helper layout + sentinel discipline + override-fires-only-on-i==0 invariant; the carveout review-follow-up moved to the closed list with the resolution rationale. Validator clean (24 skills + 7 chains; explicit-paths pass on both touched transferables + 3 proprietary skills + 2 proprietary chains all clean). (by skill-set-dev at 2026-04-27T12:30:00Z)
- Phase 19 (3) backfill landed: every open `- [ ]` SPEC item across all phases now carries a `[easy|medium|hard]` difficulty label between the markdown checkbox and the description, and every `## Next up` TODO entry likewise. Author-judgment per item — acceptance-check items typically `[easy]` (observation-only), runner code changes `[medium]`, broad multi-skill rewrites or full-chain acceptance `[hard]`. Severity tags from review (e.g. `[should-fix]`) follow the difficulty bracket so both axes coexist (e.g. `- [ ] [medium] [should-fix] ...`). Bundled the Phase 19 same-file fixes that share docs/SPEC.md as their root: (a) item #3 (this backfill) flipped `[ ]` → `[x]` with closure paragraph; (b) item #7 (`sst-dev-cycle` updates) flipped `[ ]` → `[x]` with closure paragraph noting the work shipped end-to-end in commit e48f273 (sentinel emit + don't-downgrade-or-upgrade prose at `skills/dev/sst-dev-cycle/SKILL.md:76-90`, plus `model-floor: sonnet` + `effort-floor: high` frontmatter at lines 4-5) — closes Phase 19 review-follow-up #2 (the over-claim parity fix flagged in commit 0711d17 but not yet enacted on the spec), so the follow-up bullet at the bottom of Phase 19 is rewritten to a one-line "Closed review follow-up" record. Pure prose-only change; no skill or chain YAML touched. Validator clean on the auto-walked tree (24 skills + 7 chains). No transferable touched (changes are in `docs/SPEC.md` + `docs/TODO.md`, both proprietary handoff docs in this repo's own dogfooding surface), so the sanitization gate is skipped. Future cycles route per the labels for the first time; the next-cycle pick of items #4 + #5 (runner per-skill model + effort resolution + difficulty pre-parse + sentinel capture) lands the runner-side resolution that consumes them. (by skill-set-dev at 2026-04-26T14:30:00Z)
- Phase 19 (1)+(2) same-root bundle landed end-to-end: framework now has the model + effort routing contract surface in place, with the runner-side resolution still queued for a later cycle. (a) `schema/skill-set.schema.json` gains two optional enum fields parallel to the existing frontmatter spine: `model-floor:` (enum `opus | sonnet | haiku`; default `opus` when absent; framework-canonical floor for the lowest model tier this skill is ever allowed to run on, regardless of item difficulty) and `effort-floor:` (enum `low | medium | high | xhigh | max`; default `high` matching the API-standard, with the runner's pinned `--effort high` already downshifting Opus 4.7's CLI implicit `xhigh` to that default; framework-canonical floor for reasoning-effort). Both validate declaratively via `jsonschema`'s built-in enum check; no code change in `bin/validate-frontmatter.py` was needed. (b) `templates/SPEC.md` gains a "Difficulty labels (model + effort routing)" appendix sitting just above the Phase 17 "Empty-queue bail" appendix, documenting the three labels (`[easy]`/`[medium]`/`[hard]` mapping to `(haiku, low)`/`(sonnet, medium)`/`(opus, high)`), the `effective = max(item_tier, skill_floor)` resolution rule per axis, the closed-item / Just-shipped exemption (no labels on `[x]` rows or shipped entries), and one worked code-fence example block per surface (SPEC items, TODO Next-up). The template's Phase-block example items now carry labels (`[easy]`/`[medium]`/`[hard]`) so consuming projects bootstrap with the contract visible. `templates/TODO.md` Next-up comment block extended with the label requirement + per-tier `(model, effort)` mapping + the runner-route resolution sketch + a pointer to SPEC.md's appendix for the full contract; format moves from `- <description> — <reason>` to `- [<difficulty>] <description>. Reason: <reason>`. (c) `sst-dev-cycle` v1.2.0 → v1.3.0: §1 picks up a closing "Difficulty label & sentinel emit (model + effort routing)" step that runs after the In flight line is written: parse the picked item's leading difficulty bracket, default to `medium` with `[bad-label] item missing difficulty; defaulting to medium` warned on stdout if the label is missing (graceful degradation during the contract-bump rollout window — the warn becomes a hard exit in a future cycle once the backfill is complete and most queue items carry labels), then emit `[picked-difficulty: <tier>]` on a single stdout line BEFORE the first §2 tool call. The chain runner captures that sentinel as the authoritative tier for any skill that runs after the dev (review, supervisor, etc.); the runner-side capture itself is sub-item (5), still queued. Skill prose explicitly forbids downgrading `[hard]` → `[easy]` to fit a quota and upgrading `[easy]` → `[hard]` to "be safe"; the queue-author labels are the contract. Frontmatter picks up `model-floor: sonnet` + `effort-floor: high` matching Phase 19's review-class floor table. Description's stale "in one amended commit" phrasing fixed in the same edit (queued Next-up nit cleared) — the body's §5/§6 already forbid `--amend` and document why Just-shipped omits SHAs, so the description was mid-prose drift. (d) Inline sanitize judgment pass on the sst-dev-cycle addition: terms scanned (`SPEC.md`, `TODO.md`, `## Next up`, `~/Dev/skill-set/templates/SPEC.md`, harness model/effort tier names `haiku`/`sonnet`/`opus`/`low`/`medium`/`high`, framework labels `[easy]`/`[medium]`/`[hard]`/`[bad-label]`/`[picked-difficulty:]`, generic resolution-rule pseudocode `effective_model = max(...)` / `effective_effort = max(...)`, generic frontmatter field names `model_floor` / `effort_floor`); no project nouns, no banned terms, no proprietary paths, no internal phase numbers (initial draft had "Phase 19" inline twice in transferable prose mirroring the SPEC line 281-282 should-fix flag against Phase 18 phase-number leakage; rephrased to drop the framework-internal sprint numbering before commit). Templates also got the same scrub. Verdict: must-fix=0, should-fix=0, nit=0. (e) SPEC Phase 19 items 2 and 3 (lines 316-317) flipped `[ ]` → `[x]` with closure paragraphs noting the declarative-validator coverage and the graceful-degradation warn-then-hard-fail lifecycle. Validator clean (24 skills + 7 chains; explicit-paths pass on touched transferable + chain auto-walk all clean). Remaining Phase 19 sub-items (3 backfill, 4 runner per-skill model + effort resolution, 5 difficulty pre-parse + sentinel capture, 6 dev-cycle + dev-review sentinel-on-followup wiring, 7 tag every shipped SKILL.md with floors, 8 README + CLAUDE.md docs, 9 acceptance) stay queued; (1)+(2) was the same-root pair per Phase 12 carveout, (4)+(5) bundle as the runner-changes pair next. (by skill-set-dev at 2026-04-26T13:30:00Z)
- `sst-supervisor` v1.5.0 → v1.6.0: new §0.5 "Fast-path on clean (skip §1-7 when all signals say clean)" sits before §0.6 in the doc and gates the deep walk on four eligibility conditions — (1) the immediately-preceding `supervisor_verdict.md` does NOT have an `escalate` outcome (multi-iter: prior iter's verdict; single-iter: most recent prior `.skill-runs/*/supervisor_verdict.md` other than this run's; sort by directory name since it is timestamp-prefixed); (2) every non-supervisor skill's `MANIFEST.skills[i].exit_code == 0`; (3) transcript keyword scan over every `<i>_<skill>.txt` finds no `ERROR`/`FAIL`/`Traceback`/`Exception`/`[blocker]`/`[escalate]` matches (`^\s*\[no-work\]` does NOT abort but flips the outcome label from `clean (fast-path)` to `clean (no-work bail)` since the dev skill ran but shipped no commit, so there is nothing to review); (4) §0.6 drafts sweep returns zero orphan drafts to consume (sweep runs first regardless of fast-path eligibility, since it is a cheap directory listing and the self-heal step must run unconditionally; orphans become §1 findings and abort the fast-path). On all-pass, write a minimal verdict file (full header line + `## Outcome: clean (fast-path)` or `clean (no-work bail)` + boilerplate per-skill summary noting "all skills exited 0; transcripts clean; prior verdict not escalated; no orphan drafts to consume" + `## Updates written: (none)`) and return; the §8 exit gate is satisfied because §0.6 confirmed drafts/ is empty AND the verdict file exists. On any failure, fall through to §1 with no annotation in the eventual verdict — the fast-path is an optimization, not a user-facing contract surface. Anti-fork constraint forbids softening the keyword list with `warning`/`caveat`/`should` (those appear routinely in clean prose) or adding a fifth eligibility condition without spec'ing it first; the bar is intentionally tuned to favor running the deep walk when uncertain since a missed-finding from an over-eager fast-path is a real defect while a deep-walk-on-clean is just spent compute. Saves ~$0.70-1.45 per zero-finding cycle (commonly about half of all runs once a chain is mature). SPEC Phase 12 #3 flipped `[ ]` → `[x]` with the four-condition expansion + behavior summary inlined. Sanitization judgment pass on the addition: must-fix=0, should-fix=0, nit=0 (only framework-canonical terms: `<base>/iter_<NN-1>/`, `<cwd>/.skill-runs/`, `<i>_<skill>.txt`, `MANIFEST.skills[i].exit_code`, `supervisor_verdict.md`, `## Outcome`; no project nouns, no internal phase numbers, no proprietary paths). Validator clean (24 skills + 7 chains). (by skill-set-dev at 2026-04-26T12:15:00Z)
- Two `bin/drive-chain.py` CLI UX gaps closed in one bundle. (a) New `--profile <persona>` flag: reads `<cwd>/.claude/skills/<persona>-chain-driver/SKILL.md` (then `~/.claude/skills/<persona>-chain-driver/SKILL.md` as fallback), parses the first `\`\`\`yaml` fenced block under `## Configured defaults` (keys `watched-chain`, `default-loop`, `default-max-budget-usd`, `default-max-cycles`, `telegram-env`, `label`), and applies those values as a layer BELOW CLI args (CLI wins). `--chain` is no longer strictly required when `--profile` resolves a `watched-chain:`. Mirrors what the slash-command agent does for `/<persona>-chain-driver`, so terminal users now get identical defaults via `python3 bin/drive-chain.py --profile skill-set` (no need to repeat the explicit-form 6-flag invocation). Picked option (a) from the original Next-up entry over option (b) (auto-resolve `--telegram-env` from `~/.config/<chain-stem>-telegram.env`) and option (c) (just print a hint) because (a) generalizes to any persona AND any future configured-default field, eliminates path-stem-mangling guesswork (`skill-set-cycle` → `skill-set-telegram.env` only works because the proprietary chain-driver and chain happen to share the persona prefix, not by design), and matches the slash-command agent's resolution path 1:1. (b) `--max-cycles N` silent-no-op note: at startup, if the resolved chain loop count (CLI `--loop` > chain YAML `loop:` > 1) is finite and `<= --max-cycles`, prints `[chain-driver] note: --max-cycles N is a no-op; chain '<X>' will run K iter(s) naturally and exit before the cap can fire. The cap is the safety net for multi-iter runs (--loop N>K, or chain \`loop: 0\`).` to stderr. The previous behavior was correct (cap silently never fired because the chain finished first) but discoverability was poor — caught on 2026-04-26 when user invoked `bin/drive-chain.py --chain skill-set-cycle --max-cycles 1` and observed no visible cap effect. `--help` text for `--max-cycles` now also documents it as the safety net for multi-iter runs (`--loop N>--max-cycles`, or chain `loop: 0`). Two new helpers added: `_find_profile_skill` (project-scoped → personal-global lookup), `_load_profile_defaults` (extracts the `## Configured defaults` yaml block via regex + `yaml.safe_load`, drops keys outside `PROFILE_KEYS`), `_resolve_chain_yaml_loop` (mirrors `bin/skill-chain.py`'s `load_chain` lookup order: `<cwd>/.claude/chains/` then `<repo>/chains/`; returns `loop:` field as int, defaults 1, or None if chain/yaml unresolvable so the no-op note silently skips rather than firing on a bogus chain). Proprietary `.claude/skills/skill-set-chain-driver/SKILL.md` "Invocation shape" section gains a `--profile skill-set` example block alongside the explicit form. Five end-to-end CLI smoke tests verified (profile-resolves-defaults / no-op-note-fires-when-loop-le-cap / no-op-note-suppressed-when-loop-gt-cap / missing-chain-and-profile-errors-helpfully / bogus-profile-warns-then-errors); plus seven inline unit assertions on the helpers (profile parse + missing-persona None + chain loop resolution for skill-set-cycle/dev-cycle-overnight/dev-cycle-with-review-looped/missing-chain + project-scoped wins on file lookup). Validator clean (24 skills + 7 chains; explicit-paths pass on touched proprietary chain-driver: 1 skill + 0 chains all clean). No transferable touched (changes are in `bin/drive-chain.py` and the proprietary SKILL.md), so the sanitization gate is skipped. (by skill-set-dev at 2026-04-26T11:30:00Z)
- `sst-dev-review` v1.2.0 → v1.2.1 + `skill-set-dev-review` v1.1.1 → v1.1.2: §0.2 git-state-check relaxed from "halt on any non-supervisor dirt" to "note and proceed". Both skills now capture `git status --porcelain` output verbatim, surface it as a "Working-tree state at review start" note in the §6 report and §5 commit body, then run the review against the just-shipped commit normally; dirty files stay out of `git add` per the existing §5 stage-narrowly rule. One narrow exception still halts: if the dirty file is the spec file itself or `docs/TODO.md` (the two files the review writes to in §4), stop, since concurrent writers on the same files is the one collision the note-and-proceed pattern doesn't survive. Rationale: the previous behavior wasted the cycle's commit and forced the user to babysit the working tree any time a parallel agent session, the user's own concurrent edits, or supervisor work-in-progress touched project source while a review ran. The §5 stage-narrowly rule (stage only `docs/SPEC.md` plus `docs/TODO.md` if a Next-up entry was added; never `git add -A` or `git add .`) is the structural guard ensuring the dirt cannot accidentally ride into the review commit. Transferable change passed inline sanitize judgment: must-fix=0, should-fix=0, nit=0 (only framework-canonical terms; `sst-supervisor`, `<project>-supervisor`, `<cwd>/.claude/skills/*/`, "parallel agent sessions"; no project nouns, no user-identifying details, no consumer paths). Validator clean (24 skills + 7 chains; explicit-paths pass on both touched files: 2 skills + 0 chains all clean). Closes the queued Next-up entry from the parallel session that landed `chains/dev-cycle-with-review-looped.yaml` v1.1.0. (by skill-set-dev at 2026-04-26T10:30:00Z)
- Phase 19 quick-win + spec extension to cover effort routing alongside model routing. (a) Code: `bin/skill-chain.py` `ClaudeCodeHarness.build_command` now passes `--effort high` explicitly. Opus 4.7's CLI implicit default is `xhigh` (one tier above the API-standard `high`); pinning `high` here downshifts to the API default and recovers Max-quota on routine cycles without quality risk. Neutral on Opus 4.6 / Sonnet 4.6 (where `high` is already the implicit default). Per-item difficulty-aware override layers on top once the runner work in Phase 19 lands. (b) SPEC: Phase 19 retitled from "per-skill model-tier + item-difficulty-aware routing" to "per-skill model-tier + effort + item-difficulty-aware routing"; preamble extended to explain effort scales response-token output (text + tool calls + extended thinking) so dropping `xhigh` → `high` saves ~30%, `high` → `medium` another ~30%, `medium` → `low` another ~50% on bounded mechanical tasks; combined with model-tier routing, ~25-35% of an all-Opus+all-xhigh baseline (~3-4× more iters per Max window vs the prior all-Opus baseline). Phase 19 architecture extended: SKILL.md frontmatter gains `effort-floor:` (enum `low|medium|high|xhigh|max`, default `high`) parallel to `model-floor:`; difficulty labels map to BOTH model and effort tiers; runner resolves both axes independently via `max()`; harness passes both `--model <effective_model>` and `--effort <effective_effort>`. Anti-fork rule binds at both axes. Acceptance target tightened from "~50% quota reduction" to "~25-35% of all-Opus+all-xhigh baseline (≥50% reduction)" since effort routing roughly doubles the win. The new `[x]` quick-win item lands under Phase 19 in this commit; the remaining `[ ]` items (schema, runner, tagging, docs, acceptance) are queued. (by claude-code at 2026-04-26T09:00:49Z)

## Next up (queued for next cycle)

<!--
  One line per queued item. The next cycle picks the top item unless the spec says otherwise.
  Format:
  - <one-line description> — <reason/source: spec phase X.Y, supervisor verdict <sha>, manager directive, user message>
  Order: blockers/highest-impact first.
-->

- [medium] Phase 18 `acceptance check on chain-bound worker lifecycle`. Reason: spec Phase 18 item 5; the implementation landed this cycle (item 4 `[x]`) but acceptance requires a real `/skill-set-chain-driver` round-trip with Telegram. Confirm: (a) chain driver starts the `skill-set-bot` tmux session at session-start and the user can `/ping → pong` during the run; (b) chain driver kills the tmux session at session-end; (c) re-run with the worker pre-started by hand (e.g. `tmux new-session -d -s skill-set-bot ...`): chain driver detects the pre-existing worker, does NOT start a second one, does NOT kill it at session-end; (d) two simultaneous chain-driver runs against different chains: only one tries to start the worker (flock); (e) manager invocation while no chain is running succeeds with an empty inbound queue and a digest body that does NOT re-notify any currently-paused job.
- [easy] Phase 17 `acceptance check on empty-queue bail`. Reason: spec Phase 17 item 4; the steady-state run requires `Next up` empty AND every SPEC `[ ]` flipped `[x]` (this cycle didn't reach that state, so item 4 stayed `[ ]`). When the queue eventually empties, invoke `/skill-set-chain-driver` and confirm: (a) `skill-set-dev` exits 0 with `[no-work] ...` printed and no commit; (b) chain runner aborts the loop after the first empty iter (no second iter starts; `MANIFEST.loop.terminated_by == "no_work_bail"`); (c) iter MANIFEST records `no_work_bail` with the sentinel line; (d) cumulative cost is bounded by the dev skill's pre-flight reads (~$0.10-0.30) rather than a full ~$8 iter; (e) chain-driver session-end Telegram body labels the stop "no-work bail".
- [medium] Phase 14 `Acceptance check: kill -TERM mid-supervisor + verify self-heal`. Reason: spec Phase 14 close item; one combined acceptance test for the four Phase 14 mechanisms above.
- [easy] `bin/install-skills.sh -y --force` to refresh the DIVERGED harness mirror copies of `sst-supervisor`, `sst-dev-cycle`, `sst-dev-review`. Reason: the long-queued Phase 2 follow-up plus this cycle's Phase 19 (4)+(5) closure: in consuming projects (anywhere outside this repo) the harness loads `~/.claude/skills/sst-supervisor/SKILL.md`, which is currently v1.4.1 and missing both the §0.5 fast-path (Phase 12 #3) AND the new `model-floor: opus` + `effort-floor: xhigh` floors landed in this cycle's canonical bump to v1.6.1. Until the mirror is force-refreshed, consuming projects' supervisors run the older prose at the default `opus`/`high` route rather than the declared `opus`/`xhigh` route. THIS repo is unaffected because the proprietary `.claude/skills/skill-set-supervisor/` (project-scoped) wins the `_find_skill_md` lookup and was tagged in the same cycle.
- [medium] Phase 20 `goose-cerebras harness` (deferred). Reason: spec Phase 20 (deferred behind Phase 19); the per-skill model-tier work in Phase 19 delivers the bulk of the 24/7-productivity throughput win within the existing Max subscription, so this becomes a future supplement for free-tier capacity rather than the primary fix. Re-pick when Phase 19 acceptance ships and a real cost / throughput baseline shows Phase 19's win is insufficient AND mechanical-skill work has been cleanly identified for offload to Cerebras. Architecture as in spec: `GooseCerebrasHarness` subclass + `bin/goose-shim.py` translating Goose's stream-json event vocabulary to Claude Code's shape + per-skill `allowed-harnesses:` anti-fork guard.
- [medium] Phase 12 `Acceptance: ≥25% cost reduction on multi-iteration runs vs Phase 11 baseline`. Reason: spec Phase 12 close item; a real sdrai-cycle with all four wins live, measured against the $73.59 / 9-cycle baseline. Now invocable via `bin/skill-chain.py --chain dev-cycle-with-review-looped` (shipped this cycle).
- [medium] Phase 8.6 end-to-end smoke: chain `sst-web-research → sst-editorial-pass → sst-social-promoter` against a real consuming project with clean supervisor verdict. Reason: spec Phase 8 tail item, Phase 8.1-8.5 lifts already complete.
- [easy] Phase 2 `User runs bin/install-skills.sh -y to deploy the updated sst-dev-cycle/sst-dev-review into ~/.claude/skills/`. Reason: user action, still pending.
