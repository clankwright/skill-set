# skill-set TODO (handoff doc)

> Cross-cycle state. Every skill reads this on start and updates it on close. Three sections, in this order. Primary spec: `docs/SPEC.md`.

## In flight

<!--
  Exactly one line per currently-running skill, format:
  - [<skill-name> @ <utc-iso>] <one-line: what this skill is currently doing>
  Rewrite (don't append) as the focus narrows. Empty when no skill is running.
-->

## Just shipped (last cycle)

<!--
  Append-on-close, newest first. Format:
  - <one-line summary> — by <skill-name> at <utc-iso>
  No commit SHA: a commit cannot contain its own hash, and amend-based
  workarounds produce stale references. Correlate entries to commits via
  `git log --oneline --grep '<keyword>'`. Older entries below retain their
  SHAs from the prior two-commit pattern; leave them alone, they're valid.
  Trim to the most recent 10 entries; older history lives in docs/SPEC.md
  phase blocks and `git log`.
-->

- Phase 18 review follow-up B closed: `[no-work]` bail in `bin/skill-chain.py` is now gated on a per-skill `git_sha_before` / `git_sha_after` comparison sampled around `run_skill_with_retry`. If the skill emitted the sentinel AND a commit landed during the same skill (real work shipped, sentinel was a reasoning-block false-positive), the bail is suppressed, the sentinel reason is moved onto `record["no_work_bail_suppressed"]` for forensic visibility, an in-place manifest snapshot fires so the suppression is durable in `iter_NN/MANIFEST.json`, and a DIM stderr line `[no-work] /<skill>: sentinel detected but commit shipped (<sha7> -> <sha7>); treating as false-positive, continuing` records the decision; review + supervisor then proceed normally against the commit. When either SHA is unavailable (non-git harness, `git rev-parse` timeout / FileNotFoundError), the bail still fires: the dev skill remains the authority on its own steady state and gating on a SHA we cannot read would silently break the contract on consumers that don't run under git. The new helper `_no_work_bail_should_fire(record, sha_before, sha_after)` is the decision function (returns False when no sentinel set, False when both SHAs known and differ, True otherwise); kept narrow and side-effect-free so it tests cleanly. Smoke-tested via 9 inline assertions covering: sentinel-absent + same SHA / different SHA / None SHAs (all False); sentinel + same SHA (True / fire); sentinel + different SHA (False / suppress); sentinel + None on either side (True / fire, defense for non-git harnesses); empty-string sentinel reason normalized through `handle_event` still fires. SPEC Phase 18 review-follow-up bullet B flipped `[ ]` → `[x]` with a "Fix landed" sub-paragraph documenting the helper + suppression manifest field. Validator clean (24 skills + 7 chains). No transferable touched (the change is in `bin/skill-chain.py`, not under `skills/`), so the sanitization gate is skipped. (by skill-set-dev at 2026-04-26T06:50:00Z)
- Phase 18 #4 chain-bound bot worker lifecycle landed in `bin/drive-chain.py`. Five helpers added: `_persona_from_env_file` (derives persona from `<persona>-telegram.env`; returns None on convention-mismatch so the caller skips rather than guesses); `_tmux_session_exists` (FileNotFoundError-safe so hosts without tmux just probe-miss); `_read_live_pid` (verifies via `os.kill(pid, 0)`; stale-PID-file → None so a fresh start happens; PermissionError counts as live for cross-user processes); `_probe_worker` (probes persona-suffixed tmux session, then the legacy `manager-bot` tmux name from README's always-on pattern, then `~/.claude/state/manager-bot.pid`); `_start_worker` (acquires `fcntl.flock(LOCK_EX | LOCK_NB)` on `~/.claude/state/manager-bot.pid.lock` to serialize simultaneous drivers, re-probes inside the lock for TOCTOU safety, spawns `tmux new-session -d -s <persona>-bot "TELEGRAM_ENV_FILE=<env> python3 bin/manager-bot.py"`, captures the pane PID via `tmux list-panes -F '#{pane_pid}'`, writes it to the PID file; returns None when raced / tmux missing / manager-bot.py missing rather than aborting the chain); `_stop_worker` (idempotent `tmux kill-session` + PID-file unlink). `main()` integration: start-of-session block skips entirely when `--no-telegram` OR `--telegram-env` is unset (no chat-id to ack means no worker), emits a stderr line for each branch (started / pre-existing / convention-mismatch / start-failed) so the operator sees what happened; end-of-session block runs after `proc.wait()` and before the final telegram body, gated on `worker_started_by_us` so an externally-managed worker is never killed. Probe order put `<persona>-bot` first (matches the new chain-driver-managed name), then `manager-bot` (the README always-on default; if a user is running the legacy pattern, treat as externally-managed), then PID file (covers any worker that wrote one). Smoke-tested in-process: persona derivation, tmux probe on absent name, PID file read on missing/malformed/dead-pid/live-pid paths, full `_probe_worker` on a bogus persona — all behaviors as specified, no crashes on tmux-unavailable. SPEC Phase 18 item 4 flipped `[ ]` → `[x]`; item 5 (acceptance check) carried over to Next up since it needs a live `/skill-set-chain-driver` round-trip with Telegram. Validator clean (24 skills + 7 chains). No transferable touched, so sanitization gate skipped — by skill-set-dev at 2026-04-26T05:55:00Z
- Phase 17 empty-queue handling shipped end-to-end (items 1, 2, 3 closed; item 4 acceptance still queued because it requires the actual empty-queue + clean-spec state, which this cycle didn't reach). (1) `sst-dev-cycle` v1.1.0 → v1.2.0: §0 step 6 "Empty-queue bail" added; when `TODO.md > Next up` is entry-free AND `docs/SPEC.md` has zero remaining `- [ ]` checkboxes AND the user's prompt carries no specific task, exit 0 cleanly without picking, testing, or committing, and print `[no-work] queue empty and spec fully checked; nothing to do` on stdout once before exiting. Explicit MUST-NOT block lists the four scope-creep failure modes (don't pick a just-shipped item, invent speculative work, scope-creep on existing skills, or fabricate a `Next up` entry). (2) `bin/skill-chain.py` gains `NO_WORK_SENTINEL_RE = ^\s*\[no-work\](?:\s+(.*\S))?\s*$` (multiline, line-anchored, so inline mentions like "I would emit `[no-work]` here" don't trigger because the bracket isn't at the line start). `handle_event` scans only assistant-text blocks (tool inputs / tool results bypassed, so the supervisor reading skill prose containing example sentinels never false-triggers); first match wins per skill so subsequent text doesn't overwrite. `run_iteration` checks `record.get("no_work_bail")` after a clean (rc=0) skill exit, stamps `iter_manifest["no_work_bail"] = {"skill": <name>, "reason": <line>}`, prints a BLUE `[no-work] /<skill>: <reason>: skipping remaining skills + aborting loop` banner, breaks the inner skills loop (skipping review + supervisor since there's no commit for them to operate on). `main()` sets `manifest["loop"]["terminated_by"] = "no_work_bail"` when looping and breaks the outer while loop. (3) `templates/SPEC.md` gains an "Empty-queue bail (steady state)" appendix subsection so consuming projects bootstrap with the contract documented; README.md "Loop mode" subsection extended with a sentinel-format / manifest-field / chain-driver-label paragraph; `dev-cycle-overnight` row in "Chains shipped here" notes auto-stop on empty work (budget cap becomes secondary safety net, not primary stopping criterion); CLAUDE.md "Choosing a chain" gains a one-sentence reassurance that invoking on a clean spec is safe (the SPEC phrasing assumed a "don't invoke when Next up is empty" caveat existed; none was present in this CLAUDE.md, so the edit is positive rather than removal). Three smoke tests verified: regex matches every canonical sentinel form (with/without reason, leading whitespace, multi-line context, embedded inside multi-line assistant text), rejects inline mentions and `[no-work-thing]` near-matches; `handle_event` test confirms first-match-wins and inline-mention immunity. Validator clean on the auto-walked tree (24 skills + 7 chains). Sanitization judgment pass on the `sst-dev-cycle` addition: must-fix=0, should-fix=0, nit=0 (only framework-canonical terms: `TODO.md`, `Next up`, `docs/SPEC.md`, `<project>` placeholder; no project nouns, no banned terms, no em dashes per the user's global writing-style rule). Phase 17 items 1, 2, 3 flipped `[ ]` → `[x]`; item 4 (live acceptance check) carried over to Next up. Pre-existing validator error on `.claude/skills/skill-set-chain-driver/SKILL.md` (unquoted `argument-hint: [--loop N] ...` parses as a YAML list start) noticed in passing and queued separately; not blocking the auto-walk validator and unrelated to this cycle's bundle — by skill-set-dev at 2026-04-26T05:30:00Z
- Phase 18 chain-bound worker lifecycle + manager no-spam policy landed (docs only; runtime impl queued): SPEC gains a Phase 18 block. (1) `sst-manager` v1.0.0 → v1.1.0 picks up two new Hard rules ("Never re-notify on persistent paused-job state" + a "Worker-lifecycle expectation" section explaining the chain-bound pattern). (2) `sst-chain-driver` v1.1.0 → v1.2.0 picks up a "Worker lifecycle (Phase 18; in spec, not yet implemented)" section documenting the future runtime contract: probe at session-start, start if absent, stash `worker_started_by_us`, kill at session-end iff this driver started it; leave externally-managed workers untouched. (3) CLAUDE.md "Telegram bot" section gains two new constraints (worker is chain-bound not persistent; no-re-notify on persistent paused-job state). (4) README.md "Worker management" splits into chain-bound (recommended) vs always-on (legacy) patterns. Implementation in `bin/drive-chain.py` queued at top of `Next up` for the next cycle. Validator clean (24 skills + 7 chains). Bin-helper change (drive-chain.py worker probe/start/stop with `flock` PID-file mutex against simultaneous drivers, `tmux new-session -d -s <persona>-bot ...` for the start, `tmux kill-session` for the stop) is the actual code work; this commit is documentation-only — by claude-code at 2026-04-26T03:50:00Z
- Phase 17 empty-queue handling spec'd + queued: SPEC gains a new Phase 17 block with four items: (1) `sst-dev-cycle` §0 empty-queue bail (~30 LoC prose addition; exit 0 cleanly with `[no-work] <reason>` sentinel when Next up is empty AND every SPEC `[ ]` is `[x]` AND no user task), (2) chain runner recognizes the `[no-work]` sentinel and aborts the loop entirely (manifest records `no_work_bail` + `terminated_by: no_work_bail`; chain-driver session-end Telegram labels the stop accordingly), (3) README.md + CLAUDE.md docs update (empty-queue bail is the canonical clean stop), (4) acceptance check on a real steady-state run. Items (1) and (2) queued at the TOP of `Next up` with `(group with empty-queue-bail)` same-root tags so the next `/skill-set-chain-driver` cycle bundles them per the Phase 12 #1 same-root carveout. Motivation captured in the Phase 17 preamble: today an empty-queue iter under `--loop N` burns ~$8/iter on speculative work; an overnight `--loop 0` would burn the full `--max-budget-usd` cap. The bail is correct framework behavior, not a defect; today's prose just doesn't specify it. No code change in this commit; spec + queue work only — by claude-code at 2026-04-26T03:30:00Z
- Phase 16 long-running chain pattern + chain-selection docs: shipped `chains/dev-cycle-overnight.yaml` (transferable, v1.0.0, `loop: 0` until-failure, `loop-delay-random: [300, 7200]` = 5min-2h, `auto-promote: all`, `sst-dev-cycle` → `sst-dev-review`); designed to be invoked through `sst-chain-driver` so the budget cap is the safety net. Proprietary mirror at `~/Dev/skill-set/.claude/chains/skill-set-overnight.yaml` (v1.0.0, same shape with `skill-set-*` skills + auto-appended `skill-set-supervisor`; lives under `.claude/chains/` per the gitignored runtime-state pattern). README.md gains a "Chains shipped here" subsection: a table mapping every transferable chain to its loop count, auto-promote mode, and use case, followed by a "Pick the dev chain by intent" guide (one change → `dev-cycle-with-review`; 1-3 items → `dev-cycle-with-review-looped` via `sst-chain-driver`; overnight drain → `dev-cycle-overnight` via `sst-chain-driver` with `--max-budget-usd $X`). README.md "Loop mode" subsection extended with the `loop: 0` semantic ("until failure / Ctrl-C, intended for `sst-chain-driver`-wrapped overnight runs where the budget cap is the natural stopping criterion"). CLAUDE.md gains a "Choosing a chain" section pointing to `/skill-set-chain-driver` (canonical default), `/skill-set-chain-driver --chain skill-set-overnight --max-budget-usd 80` (overnight drain), and raw `bin/skill-chain.py --chain skill-set-cycle` (debug mode without driver wrap). Proprietary `skill-set-chain-driver` SKILL.md "Common overrides" list extended with the overnight invocation pattern. SPEC Phase 16 block added documenting all five sub-items as `[x]`. Validator clean (24 skills + 7 chains; transferable chain count up by one) — by claude-code at 2026-04-26T03:05:00Z
- Telegram bot operational for skill-set. `/sst-setup-telegram` provisioned `@skillset_ops_bot` (id 8373198106) DM-allowlisted to one private chat, env file at `~/.config/skill-set-telegram.env` (mode 600, owner rob; matches the `telegram-env:` path declared in `~/Dev/skill-set/.claude/skills/skill-set-chain-driver/SKILL.md:26`), long-poll worker `bin/manager-bot.py` running in tmux session `manager-bot`. Eight bot commands registered via `setMyCommands` (status, objectives, proposals, promote, pause, resume, ping, help). Outbound round-trip verified through `bin/notify-telegram.sh`; inbound `/ping → pong` verified before worker installation; worker drained the backlog on startup as expected (pre-install non-command messages got "I only understand commands" replies; `/ping` got `pong`). Path-mismatch caught and fixed before docs landed: skill default was `~/.config/telegram-<project>.env` → `telegram-skillset.env`, but the proprietary chain-driver SKILL.md declared `~/.config/skill-set-telegram.env`; renamed and worker relaunched so `/skill-set-chain-driver` picks up the credentials without a SKILL.md edit. README.md gains a new "Telegram bot" section between "Chain YAML fields > Rate-limit handling" and "Selecting a harness" covering setup (`/sst-setup-telegram`), daily use (outbound via `notify-telegram.sh`; inbound `/help`/`/status`/etc.), worker management (tmux / systemd-user / rc.d), and three caveats (single `getUpdates` consumer; sleep vs hibernate vs `wsl --shutdown`; long-poll only, no webhook). Project CLAUDE.md gains a "Telegram bot" section with five AI-facing constraints (don't read env files; outbound via `notify-telegram.sh`; one `getUpdates` consumer; queue-dir is the contract; env-file naming convention `~/.config/<persona>-telegram.env`). Caught a small skill-prose vs shipped-helper mismatch in `sst-setup-telegram` §5; queued in Next up. No SPEC change: bot setup is operational state, not a framework-spec phase item; Phase 5 already had `bin/manager-bot.py` + `bin/notify-telegram.sh` + service-unit / rc.d templates shipped — by sst-setup-telegram at 2026-04-26T02:20:00Z
- Phase 13 acceptance closed + proprietary `skill-set-chain-driver` shipped: SPEC Phase 13 acceptance flipped `[ ]` → `[x]` based on the live `2026-04-25T13-36-00Z_skill-set-cycle/iter_03` run (five_hour `rejected` at 14:47:12Z; `source: rate_limit_event`; sleep 6811.5s = `reset_time - detected + jitter[15,60]s` within ±60s of the parsed reset; retry session `14f48ecd` started at 16:40:44Z matching `wake_at` exactly, drift 0s; retried skill ran 28 turns / 377s / $2.025 / `exit_code: 0`; chain finished all 3 iters and committed `a45a506`; `MANIFEST.rate_limit_pauses[0]` records the full timeline; original session archived to `00_skill-set-dev.retry-0.{txt,jsonl}` per the per-attempt rotation). Original criterion (f) Telegram pause/resume notifications rolled into Phase 12 chain-driver acceptance (the wiring is in `bin/drive-chain.py` and only fires when invoked via `sst-chain-driver`); not blocking Phase 13 closure. Proprietary `~/Dev/skill-set/.claude/skills/skill-set-chain-driver/SKILL.md` (v1.0.0, `transferable: sst-chain-driver`, `transferable-version: ">=1.1.0"`) wraps the transferable with skill-set defaults: `watched-chain: skill-set-cycle`, `default-loop: 3`, `default-max-budget-usd: 30.00` (~2× the empirical $15.5 / 3-iter spend), `default-max-cycles: 3`, `telegram-env: ~/.config/skill-set-telegram.env`, `label: skill-set`. User now types `/skill-set-chain-driver` (no flags) for the canonical run. Telegram env file does not yet exist; `/sst-setup-telegram` is the one-shot provisioning step that creates it. Closes the Phase 12 #5 tail item (`<persona>-orchestrator → <persona>-chain-driver` proprietary counterpart) for skill-set; the `Next up` line is retired. Validator clean (24 skills + 6 chains; the proprietary lives under `.claude/skills/`, not `skills/`, so it doesn't increase the transferable count) — by claude-code at 2026-04-26T02:25:00Z
- Phase 15 rename for clarity: `sst-orchestrator` → `sst-chain-driver` (single-session driver of one multi-iter chain run; v1.0.0 → v1.1.0); `sst-agent-orchestrator` → `sst-skill-router` (in-process per-task planner inside one user request; v1.0.0 → v1.1.0); helper `bin/orchestrate-chain.py` → `bin/drive-chain.py`. `sst-manager` unchanged (cross-project cron-based ops loop fits the everyday meaning of "manager"). Motivation: "orchestrator" appearing on two distinct skills made conversational references ambiguous and the everyday English meaning overlapped with both. New names lead with mechanism (drive a chain run / route to skills / monitor projects) rather than a management metaphor. SKILL.md frontmatter `name:` updated; body prose rewritten (skill heading, "the orchestrator" prose throughout, hard-rules block, command-line shape examples); both renamed skills carry a "Naming history" footer. `bin/drive-chain.py` internal references updated (docstring, `prog=` argparse name, all `[orchestrator]` log-line tags → `[chain-driver]`, runtime Telegram body prefixes `chain-driver: session START` / `chain-driver [<label>]: ...` / `chain-driver: session END`). `sst-email-control-loop` description's "called by sst-agent-orchestrator" reference updated to point at `sst-skill-router`. Forward-looking SPEC.md items in Phase 12 + Phase 13 acceptance updated to new names; historical closed Phase 12 #5 / Phase 13 #6 entries left intact (the historical record is correct as written). New Phase 15 SPEC block documents the rename + rationale + migration list. Stale deployed copies at `~/.claude/skills/sst-orchestrator/` + `~/.claude/skills/sst-agent-orchestrator/` removed manually after `bin/install-skills.sh -y` (the install script intentionally does not delete target-only dirs to protect hand-managed skills, so renames require an explicit cleanup step). Validator clean (24 skills + 6 chains). No proprietary `<persona>-orchestrator` counterpart existed in any consuming project, so no external cascade — by claude-code at 2026-04-26T01:55:00Z
- Phase 6 push-to-GitHub closed: `origin` set to `git@github.com:toadlyBroodle/skill-set.git`, `git push -u origin main` published all history through `a45a506`, `main` now tracks `origin/main`. Closes the standing supervisor "push step is no-op" observation that's been carried in `Next up` since the framework gained an `install-skills.sh` push call. SPEC Phase 6 `Push to public GitHub repo` flipped `[ ]` → `[x]`. CONTRIBUTING.md and CI items in Phase 6 already `[x]` from prior cycles. No code change; remote add + push only — by claude-code at 2026-04-26T01:27:00Z

## Next up (queued for next cycle)

<!--
  One line per queued item. The next cycle picks the top item unless the spec says otherwise.
  Format:
  - <one-line description> — <reason/source: spec phase X.Y, supervisor verdict <sha>, manager directive, user message>
  Order: blockers/highest-impact first.
-->

- Phase 18 `acceptance check on chain-bound worker lifecycle` — reason: spec Phase 18 item 5; the implementation landed this cycle (item 4 `[x]`) but acceptance requires a real `/skill-set-chain-driver` round-trip with Telegram. Confirm: (a) chain driver starts the `skill-set-bot` tmux session at session-start and the user can `/ping → pong` during the run; (b) chain driver kills the tmux session at session-end; (c) re-run with the worker pre-started by hand (e.g. `tmux new-session -d -s skill-set-bot ...`): chain driver detects the pre-existing worker, does NOT start a second one, does NOT kill it at session-end; (d) two simultaneous chain-driver runs against different chains: only one tries to start the worker (flock); (e) manager invocation while no chain is running succeeds with an empty inbound queue and a digest body that does NOT re-notify any currently-paused job.
- Phase 17 `acceptance check on empty-queue bail` — reason: spec Phase 17 item 4; the steady-state run requires `Next up` empty AND every SPEC `[ ]` flipped `[x]` (this cycle didn't reach that state, so item 4 stayed `[ ]`). When the queue eventually empties, invoke `/skill-set-chain-driver` and confirm: (a) `skill-set-dev` exits 0 with `[no-work] ...` printed and no commit; (b) chain runner aborts the loop after the first empty iter (no second iter starts; `MANIFEST.loop.terminated_by == "no_work_bail"`); (c) iter MANIFEST records `no_work_bail` with the sentinel line; (d) cumulative cost is bounded by the dev skill's pre-flight reads (~$0.10-0.30) rather than a full ~$8 iter; (e) chain-driver session-end Telegram body labels the stop "no-work bail".
- `.claude/skills/skill-set-chain-driver/SKILL.md` `argument-hint:` field is parsed as a YAML list — reason: noticed during Phase 17 validator pass when explicit-paths invocation surfaced `expected <block end>, but found '['` at column 27 of line 7. The field reads `argument-hint: [--loop N] [--max-budget-usd $X] [--max-cycles N]`; PyYAML treats the bare `[--loop N]` as a flow-sequence start. Auto-walk validator misses this because `.claude/skills/` is outside the canonical `skills/` walk. Two fixes: (a) quote the value (`argument-hint: "[--loop N] [--max-budget-usd $X] [--max-cycles N]"`); (b) drop the field if the proprietary skill doesn't reference it elsewhere. Trivial proprietary-prose nit; bundle with any other small `skill-set-chain-driver` touch-up. Outside the `skills/` carve-out for the auto-walk validator so it can be picked up via the .claude carve-out path on the next pre-flight, but does NOT block the auto-walk validator.
- Phase 14 `Acceptance check: kill -TERM mid-supervisor + verify self-heal` — reason: spec Phase 14 close item; one combined acceptance test for the four Phase 14 mechanisms above.
- Phase 12 `sst-supervisor fast-path on clean` — reason: spec Phase 12 efficiency-win 2; ~5 of 9 sdrai cycles produced no findings yet supervisor still spent $0.97–1.45 each. §0.5 keyword-scan pre-check + clean-deploy verification short-circuits to a one-line verdict; never applies when prior verdict says escalate.
- `sst-dev-cycle` description frontmatter says "in one amended commit" — reason: stale prose, contradicts §5/§6 of the body which both forbid `--amend` and document why the Just-shipped line omits SHAs. Fix: remove the word "amended" so description reads "in one commit". Trivial prose nit; bundle with any other small `sst-dev-cycle` prose touch-up.
- `sst-setup-telegram` §5 prose vs `bin/notify-telegram.sh` interface mismatch — reason: skill §5 documents `echo "..." | TELEGRAM_ENV_FILE=<env-path> bash <repo>/bin/notify-telegram.sh` for the outbound test, but the shipped helper requires `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` already in the environment and does NOT auto-source `TELEGRAM_ENV_FILE`. Caught during the skill-set bot setup on 2026-04-26 (the first invocation errored with `TELEGRAM_BOT_TOKEN: TELEGRAM_BOT_TOKEN is required`; workaround is `( set -a; . <env-path>; set +a; echo "..." | bash bin/notify-telegram.sh )`). Two clean fixes, pick one: (a) rewrite skill §5 to source the env first; (b) teach `notify-telegram.sh` to auto-source `$TELEGRAM_ENV_FILE` when set. Option (b) is the more robust one: every consumer benefits and the skill prose doesn't need any change. Trivial; bundle with any other small `sst-setup-telegram` or `notify-telegram.sh` touch-up.
- Phase 12 `Acceptance: ≥25% cost reduction on multi-iteration runs vs Phase 11 baseline` — reason: spec Phase 12 close item; a real sdrai-cycle with all four wins live, measured against the $73.59 / 9-cycle baseline. Now invocable via `bin/skill-chain.py --chain dev-cycle-with-review-looped` (shipped this cycle).
- Phase 8.6 end-to-end smoke: chain `sst-web-research → sst-editorial-pass → sst-social-promoter` against a real consuming project with clean supervisor verdict — reason: spec Phase 8 tail item, Phase 8.1-8.5 lifts already complete.
- Phase 2 `User runs bin/install-skills.sh -y to deploy the updated sst-dev-cycle/sst-dev-review into ~/.claude/skills/` — reason: user action, still pending.
